/*__DW_FORTRESS_COMMAND_EXPANSION__*/
// Dwarf World — Fortress Command & Identity Expansion runtime.
// Injected inside the existing DwarfWorld-core IIFE by the shell loader.

const DWX_VERSION=1;
const DWX_BUILDINGS={
  great_hall:{name:"Great Hall",icon:"🏛️",cost:420,depth:0,range:8,desc:"Raises dwarf capacity, reduces later recruitment costs, and unlocks civic construction."},
  tavern:{name:"Tavern",icon:"🍺",cost:280,depth:0,range:8,desc:"Rest, recovery and visiting recruit candidates."},
  treasury:{name:"Treasury",icon:"💰",cost:460,depth:120,range:6,desc:"Increases the value of treasure returned to civilization."},
  barracks:{name:"Barracks",icon:"⚔️",cost:520,depth:120,range:10,desc:"Military staging with modest local attack and defense support."},
  workshop:{name:"Workshop",icon:"🛠️",cost:500,depth:120,range:7,desc:"Makes forge work and later construction slightly cheaper."},
  mushroom_farm:{name:"Mushroom Farm",icon:"🍄",cost:390,depth:260,range:9,desc:"Produces Deepcap Rations that help nearby expeditions recover and move."},
  infirmary:{name:"Infirmary",icon:"✚",cost:560,depth:260,range:11,desc:"Strong healing and an emergency rescue cooldown for critically wounded dwarves."},
  survey_hall:{name:"Survey Hall",icon:"🧭",cost:680,depth:420,range:28,desc:"Reports approximate valuable formations and danger beyond explored tunnels."},
  lift_shaft:{name:"Lift Shaft",icon:"↕️",cost:900,depth:420,range:3,desc:"Paired rapid travel for long deep-expedition routes."},
  shrine:{name:"Shrine of the Ancestors",icon:"🕯️",cost:820,depth:620,range:10,desc:"Veterans can become Inspired and earn a small experience bonus."},
  great_vault:{name:"Great Vault",icon:"🔐",cost:1050,depth:860,range:5,desc:"Protects elite equipment and slightly improves unique relic discovery."}
};
const DWX_BUILD_COL={great_hall:"#e5b95f",tavern:"#d99560",treasury:"#f6ce58",barracks:"#d27b66",workshop:"#9eabba",mushroom_farm:"#b490ea",infirmary:"#79dfa9",survey_hall:"#78dcff",lift_shaft:"#b5c1cf",shrine:"#d8a6ff",great_vault:"#ffd66f"};
const DWX_RELICS=[
  {id:"worldsplitter",name:"WORLDSPLITTER",category:"pickaxe",flavor:"The mountain remembers the first wound.",stats:{miningPower:34,miningRate:1.48},effect:"Destroyed rock can fracture nearby stone in a 3×3 area."},
  {id:"heart_of_mountain",name:"HEART OF THE MOUNTAIN",category:"pickaxe",flavor:"It beats harder where the sun has never reached.",stats:{miningPower:27,miningRate:1.35},effect:"Mining power increases with depth."},
  {id:"fortunes_end",name:"FORTUNE'S END",category:"pickaxe",flavor:"Every stone owes the delver a secret.",stats:{miningPower:24,miningRate:1.30},effect:"Returned ore is more valuable and ordinary rock can reveal gemstones."},
  {id:"king_under_stone",name:"KING UNDER STONE",category:"weapon",weaponType:"axe",flavor:"The crown vanished. The axe remembered.",stats:{dmg:46,range:1.35,rate:1.08,ranged:false},effect:"Damage scales with the wielder's level."},
  {id:"spawnbreaker",name:"SPAWNBREAKER",category:"weapon",weaponType:"axe",flavor:"Nests learned to fear a name.",stats:{dmg:42,range:1.35,rate:1.12,ranged:false},effect:"Deals greatly increased damage while fighting close to Deepwild spawners."},
  {id:"deepward_bow",name:"DEEPWARD BOW",category:"weapon",weaponType:"bow",flavor:"Its string hums loudest beyond the lamps.",stats:{dmg:30,range:12.5,rate:1.34,ranged:true},effect:"Damage increases far from the fortress."},
  {id:"grudgeplate",name:"GRUDGEPLATE",category:"armor",armorTier:"titanium",flavor:"Every scar is entered into the ledger.",stats:{defense:.48,bonusHp:135},effect:"Major kills record Grudges that grant a small permanent defense bonus."},
  {id:"last_kings_bulwark",name:"LAST KING'S BULWARK",category:"armor",armorTier:"titanium",flavor:"A kingdom may fall. Its last guard does not.",stats:{defense:.52,bonusHp:170},effect:"A lethal hit can leave the wearer at 1 HP and return them toward safety. Long cooldown."},
  {id:"armor_deep_road",name:"ARMOR OF THE DEEP ROAD",category:"armor",armorTier:"steel",flavor:"Made for roads no map admits exist.",stats:{defense:.42,bonusHp:120},effect:"Movement speed rises far from the fortress."},
  {id:"lantern_first_delver",name:"LANTERN OF THE FIRST DELVER",category:"armor",armorTier:"steel",flavor:"The first light below still refuses to die.",stats:{defense:.32,bonusHp:90},effect:"Expands fog-of-war vision and helps expose deep resources."},
  {id:"voidhook",name:"VOIDHOOK",category:"armor",armorTier:"titanium",flavor:"When the Deep closes its hand, the hook pulls back.",stats:{defense:.38,bonusHp:110},effect:"Critical health can pull the wearer to a nearby safe explored tile. Long cooldown."},
  {id:"ancestors_token",name:"ANCESTOR'S TOKEN",category:"armor",armorTier:"steel",flavor:"A thousand names travel with the bearer.",stats:{defense:.28,bonusHp:75},effect:"The owner gains additional experience."}
];
function dwxFresh(){return{v:DWX_VERSION,buildings:[],protectedIds:[],relicsFound:[],presentedRelics:[],surveys:[],maxDepth:0,latestDiscovery:null,visitorReady:false,visitorAt:0,stats:{goldBonus:0,relicDrops:0}}}
let dwx=dwxFresh();
let dwxBuildType=null,dwxNeedFilter="all",dwxInvMode="all",dwxMapOpen=false,dwxLastMap=0,dwxLastCivic=0,dwxLastSurvey=0;
const dwxBuildings=t=>dwx.buildings.filter(b=>b.type===t);
const dwxCount=t=>dwxBuildings(t).length;
const dwxProtectedSet=()=>new Set(dwx.protectedIds||[]);
const dwxIsProtected=it=>!!it&&(!!it.relicId||dwxProtectedSet().has(it.id));
const dwxDeepest=()=>Math.max(dwx.maxDepth||0,...dwarves.map(d=>depthAt(Math.round(d.x),Math.round(d.y))),...dwx.buildings.map(b=>depthAt(b.x,b.y)));
const dwxHasHall=()=>dwxCount("great_hall")>0;
const dwxPopCap=()=>12+dwxBuildings("great_hall").reduce((n,b)=>n+(b.level>=3?16:b.level===2?8:3),0);
const dwxWorkshopDiscount=()=>Math.min(.10,dwxBuildings("workshop").reduce((n,b)=>n+(b.level>=3?.05:b.level===2?.03:.015),0));
const dwxTreasuryBonus=()=>Math.min(.10,dwxBuildings("treasury").reduce((n,b)=>n+(b.level>=3?.10:b.level===2?.06:.03),0));
const dwxVaultBonus=()=>Math.min(.018,dwxCount("great_vault")*.006);
function dwxNearest(type,x,y){let best=null,bd=Infinity;for(const b of dwxBuildings(type)){const q=dist2(x,y,b.x,b.y);if(q<bd){bd=q;best=b}}return best?{building:best,distance:Math.sqrt(bd)}:null}
function dwxProtect(id,on=true){const s=dwxProtectedSet();on?s.add(id):s.delete(id);dwx.protectedIds=[...s];markUI("inv")}
function dwxBuildingCost(type){const d=DWX_BUILDINGS[type];return Math.max(1,Math.round(d.cost*(1-dwxWorkshopDiscount())))}
function dwxUnlocked(type){const d=DWX_BUILDINGS[type];return !!d&&dwxDeepest()>=d.depth&&(type==="great_hall"||type==="tavern"||dwxHasHall())}

(function dwxStyles(){
 const s=document.createElement("style");s.id="dwxStyles";s.textContent=`
 #dwarfList{gap:7px;border:0;background:transparent;overflow:visible}.dwx-card{position:relative!important;display:grid!important;grid-template-columns:42px 1fr!important;gap:8px!important;min-height:112px!important;padding:9px!important;border:1px solid rgba(255,255,255,.10)!important;border-radius:12px!important;background:linear-gradient(180deg,rgba(255,255,255,.055),rgba(0,0,0,.18))!important;overflow:hidden!important}.dwx-card.sel{border-color:rgba(120,220,255,.48)!important;box-shadow:0 0 0 1px rgba(120,220,255,.12) inset}.dwx-avatar{width:42px;height:48px;border-radius:10px;position:relative;background:linear-gradient(180deg,#80705e,#312b28);border:1px solid rgba(255,255,255,.16);overflow:hidden}.dwx-avatar:before{content:"";position:absolute;width:22px;height:22px;border-radius:50%;background:#d5a879;left:9px;top:9px;box-shadow:0 -6px 0 2px #7a838d}.dwx-avatar:after{content:"";position:absolute;width:24px;height:18px;border-radius:2px 2px 12px 12px;background:var(--beard,#8d5c38);left:8px;top:25px;clip-path:polygon(0 0,100% 0,80% 100%,50% 76%,20% 100%)}.dwx-main{min-width:0;text-align:left}.dwx-name{display:block;font-size:12px;font-weight:1000;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.dwx-act{display:block;font-size:10px;color:rgba(231,238,247,.72);margin-top:2px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.dwx-gear{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:4px;margin-top:6px}.dwx-slot{padding:4px;border:1px solid rgba(255,255,255,.08);border-radius:7px;background:rgba(0,0,0,.16);font-size:9px;text-align:center;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.dwx-slot.empty{border-color:rgba(255,110,120,.28);color:#ffabb5;background:rgba(255,70,90,.07)}.dwx-perks{display:flex;gap:3px;margin-top:5px;min-height:16px}.dwx-perk{width:16px;height:16px;border-radius:50%;display:grid;place-items:center;background:rgba(255,214,111,.10);border:1px solid rgba(255,214,111,.22);font-size:9px}.dwx-hp{position:absolute;left:0;right:0;bottom:0;height:4px;background:rgba(255,255,255,.06)}.dwx-hp i{display:block;height:100%;background:linear-gradient(90deg,#ff6f79,#f1cc62,#7be0a4)}
 .dwx-need{display:grid;gap:7px;margin:8px 0}.dwx-need-head{display:flex;justify-content:space-between;align-items:center}.dwx-chiprow,.dwx-inv-modes{display:flex;gap:4px;flex-wrap:wrap}.dwx-chip{height:28px;padding:0 8px;border-radius:999px;font-size:10px}.dwx-chip.on{background:rgba(120,220,255,.15);border-color:rgba(120,220,255,.38)}#dwxAutoEquip{height:36px;width:100%;border-color:rgba(215,171,84,.42);background:rgba(215,171,84,.12)}
 .dwx-buildgrid{display:grid;grid-template-columns:1fr 1fr;gap:6px}.dwx-build{height:auto;min-height:56px;padding:7px;text-align:left}.dwx-build b{display:block;font-size:10px}.dwx-build span{display:block;margin-top:2px;font-size:9px;color:rgba(231,238,247,.6)}.dwx-build.locked{opacity:.48}.dwx-build.on{background:rgba(120,220,255,.14);outline:1px solid rgba(120,220,255,.5)}
 #dwxMap{position:fixed;right:12px;top:80px;width:230px;z-index:45;border:1px solid rgba(255,255,255,.14);border-radius:14px;background:rgba(7,10,14,.91);box-shadow:0 16px 40px rgba(0,0,0,.42);overflow:hidden;backdrop-filter:blur(10px)}#dwxMap.closed .dwx-mapbody{display:none}.dwx-maphead{display:flex;align-items:center;justify-content:space-between;padding:7px 8px;border-bottom:1px solid rgba(255,255,255,.08);font-size:11px;font-weight:1000}.dwx-mapbody{padding:7px}.dwx-mapwrap{height:190px;border-radius:9px;overflow:hidden;border:1px solid rgba(255,255,255,.08);background:#030507}#dwxMini{width:100%;height:100%;display:block;image-rendering:pixelated;cursor:pointer}.dwx-mapnav{display:grid;grid-template-columns:1fr 1fr;gap:4px;margin-top:6px}.dwx-mapnav button{height:28px;padding:0 4px;font-size:9px}
 #dwxPlaceInfo{position:fixed;left:50%;bottom:18px;transform:translateX(-50%);z-index:58;display:none;min-width:270px;max-width:min(440px,92vw);padding:9px 11px;border-radius:12px;border:1px solid rgba(255,255,255,.15);background:rgba(8,11,15,.93);font-size:10px;box-shadow:0 14px 36px rgba(0,0,0,.42);pointer-events:none}.dwx-q{font-weight:1000;letter-spacing:.08em}.dwx-q.excellent{color:#7be0a4}.dwx-q.good{color:#78dcff}.dwx-q.poor{color:#f1cc62}.dwx-q.invalid{color:#ff7d8e}
 #dwxRelicModal{position:fixed;inset:0;z-index:110;display:none;place-items:center;background:rgba(2,3,6,.78);backdrop-filter:blur(8px)}#dwxRelicModal.open{display:grid}.dwx-reliccard{width:min(520px,92vw);padding:22px;border-radius:18px;border:1px solid rgba(255,214,111,.48);background:radial-gradient(circle at 50% 0,rgba(255,214,111,.14),transparent 44%),linear-gradient(180deg,#171716,#08090b);box-shadow:0 0 60px rgba(255,214,111,.14),0 30px 80px #000}.dwx-relictag{text-align:center;color:#ffd66f;font-size:11px;font-weight:1000;letter-spacing:.22em}.dwx-relicname{text-align:center;font-size:25px;font-weight:1000;margin:8px 0}.dwx-relicflavor{text-align:center;color:#baa989;font-style:italic;font-size:12px}.dwx-relicstats{margin:14px 0;padding:10px;border-radius:10px;background:rgba(255,255,255,.04);font-size:11px;line-height:1.55}.dwx-relicactions{display:flex;justify-content:center;gap:7px;flex-wrap:wrap}.dwx-protect{height:27px!important;padding:0 6px!important;font-size:10px!important}.dwx-protect.on{color:#ffd66f;border-color:rgba(255,214,111,.4)}
 @media(max-width:820px),(pointer:coarse){#dwxMap{left:6px;right:6px;top:54px;bottom:60px;width:auto}#dwxMap.closed{left:auto;right:8px;bottom:auto;width:120px}.dwx-mapwrap{height:min(62vh,520px)}.dwx-buildgrid{grid-template-columns:1fr}#dwxPlaceInfo{bottom:64px}}
 `;document.head.appendChild(s)
})();

function dwxActivity(d){const dep=depthAt(Math.round(d.x),Math.round(d.y));if(d.hp<=d.maxHp*.22)return`Fleeing — Depth ${dep}`;if(d.carrying)return`Returning Loot — Depth ${dep}`;if(d.guardPostId)return`Guarding Fortress — Depth ${dep}`;if(d.weapon&&enemies.some(e=>dist2(d.x,d.y,e.x,e.y)<80))return`Fighting — Depth ${dep}`;if(d.pickaxe&&d.path?.length)return`Mining Frontier — Depth ${dep}`;if(d.path?.length)return`Traveling — Depth ${dep}`;if(dep<18)return"At the Fortress";return`Exploring — Depth ${dep}`}
function dwxGear(icon,it){if(!it)return`<span class="dwx-slot empty">${icon} EMPTY</span>`;const r=it.relicId?"✦":String(it.rarity||"?").slice(0,1).toUpperCase();return`<span class="dwx-slot" title="${it.name||"Equipment"}">${icon} ${r}</span>`}
function dwxNeedCounts(){const n={armor:0,weapon:0,pickaxe:0};for(const d of dwarves)for(const k of Object.keys(n))if(!d[k])n[k]++;return n}
function dwxUpdateNeed(){const root=document.getElementById("dwxNeed");if(!root)return;const n=dwxNeedCounts();root.querySelector("[data-need=armor]").textContent=`🛡 ${n.armor}`;root.querySelector("[data-need=weapon]").textContent=`⚔ ${n.weapon}`;root.querySelector("[data-need=pickaxe]").textContent=`⛏ ${n.pickaxe}`;root.querySelector(".dwx-needlabel").textContent=n.armor+n.weapon+n.pickaxe?"Equipment Needed":"✓ All dwarves equipped"}
updateDwarfListUI=function(){UI.dwarfList.innerHTML="";for(const d of dwarves){if(dwxNeedFilter!=="all"&&d[dwxNeedFilter])continue;const b=document.createElement("button");b.type="button";b.dataset.dwarfId=d.id;b.className="dwarfBtn dwx-card"+((selected.kind==="dwarf"&&selected.id===d.id)?" sel":"");const seed=Array.from(String(d.id||d.name||"d")).reduce((a,c)=>a+c.charCodeAt(0),0),beards=["#7b4a2b","#9b6a3a","#c7c7c7","#332d2a","#caa06a"],perks=(d.perks||[]).slice(0,6).map(id=>`<span class="dwx-perk" title="${PERKS[id]?.name||id}">✦</span>`).join(""),hp=clamp(d.hp/Math.max(1,d.maxHp)*100,0,100);b.innerHTML=`<span class="dwx-avatar" style="--beard:${beards[seed%beards.length]}"></span><span class="dwx-main"><span class="dwx-name">${d.name} · Lv. ${d.level}</span><span class="dwx-act">${dwxActivity(d)} · ❤️ ${Math.round(d.hp)}/${d.maxHp}</span><span class="dwx-gear">${dwxGear("🛡",d.armor)}${dwxGear("⚔",d.weapon)}${dwxGear("⛏",d.pickaxe)}</span><span class="dwx-perks">${perks}</span></span><span class="dwx-hp"><i style="width:${hp}%"></i></span>`;UI.dwarfList.appendChild(b)}UI.dwCount.textContent=dwxNeedFilter==="all"?String(dwarves.length):`${UI.dwarfList.childElementCount}/${dwarves.length}`;dwxUpdateNeed()};
function dwxVisibleOrder(){const ids=[...UI.dwarfList.querySelectorAll("[data-dwarf-id]")].map(b=>b.dataset.dwarfId),list=ids.map(id=>dwarves.find(d=>String(d.id)===String(id))).filter(Boolean);return list.length?list:dwarves.slice()}
function dwxAutoEquip(){const order=dwxVisibleOrder(),count={armor:0,weapon:0,pickaxe:0};for(const cat of ["armor","weapon","pickaxe"]){const targets=order.filter(d=>!d[cat]),items=loot.filter(i=>i.category===cat).sort((a,b)=>itemScore(b)-itemScore(a));for(let i=0;i<Math.min(targets.length,items.length);i++){const d=targets[i],it=items[i],ix=loot.findIndex(x=>x.id===it.id);if(ix<0||d[cat])continue;loot.splice(ix,1);d[cat]=it;if(it.relicId)dwxProtect(it.id,true);applyDwarfStats(d);count[cat]++}}loot.sort(sortLoot);markUI("all");const n=dwxNeedCounts();toast(`Auto Equipped — ${count.weapon} Weapons • ${count.armor} Armor • ${count.pickaxe} Pickaxes • ${n.armor+n.weapon+n.pickaxe} empty slots remain`)}

(function dwxRosterControls(){const card=UI.dwarfList.closest(".card");if(!card)return;const root=document.createElement("div");root.id="dwxNeed";root.className="dwx-need";root.innerHTML=`<div class="dwx-need-head"><b class="dwx-needlabel">Equipment Needed</b><span class="tiny">filter missing slots</span></div><div class="dwx-chiprow"><button class="dwx-chip on" data-need="all">All</button><button class="dwx-chip" data-need="armor">🛡 0</button><button class="dwx-chip" data-need="weapon">⚔ 0</button><button class="dwx-chip" data-need="pickaxe">⛏ 0</button></div><button id="dwxAutoEquip" type="button">⭐ Auto Equip Empty Slots</button>`;card.insertBefore(root,UI.dwarfList);root.addEventListener("click",e=>{const f=e.target.closest("[data-need]");if(f){dwxNeedFilter=f.dataset.need;root.querySelectorAll("[data-need]").forEach(x=>x.classList.toggle("on",x===f));markUI("dwarves")}});root.querySelector("#dwxAutoEquip").addEventListener("click",dwxAutoEquip);dwxUpdateNeed()})();

const dwxBaseSellItem=sellItemById;sellItemById=function(id){const it=loot.find(x=>x.id===id);if(dwxIsProtected(it)){toast("Protected equipment cannot be sold.");return}return dwxBaseSellItem(id)};
function dwxSafeSell(fn){const held=loot.filter(dwxIsProtected);if(!held.length)return fn();for(const it of held){const i=loot.indexOf(it);if(i>=0)loot.splice(i,1)}try{return fn()}finally{loot.push(...held);loot.sort(sortLoot);markUI("inv")}}
const dwxBaseSellAll=sellAll,dwxBaseSellJunk=sellJunk;sellAll=function(){return dwxSafeSell(dwxBaseSellAll)};sellJunk=function(){return dwxSafeSell(dwxBaseSellJunk)};
function dwxInvPass(it){if(dwxInvMode==="all")return true;if(dwxInvMode==="needed")return dwarves.some(d=>!d[it.category]);if(dwxInvMode==="relics")return!!it.relicId;if(dwxInvMode==="legendary")return!!it.relicId||it.rarity==="legendary";if(dwxInvMode==="protected")return dwxIsProtected(it);if(dwxInvMode==="upgrades"){const d=getSelectedDwarf();return!!d&&itemScore(it)>itemScore(d[it.category])}return true}
const dwxBaseRecompute=recomputeInvFiltered;recomputeInvFiltered=function(){dwxBaseRecompute();if(dwxInvMode!=="all"){invFiltered=invFiltered.filter(dwxInvPass);UI.invSpacer.style.height=invFiltered.length*INV_ROW_H+"px"}};
const dwxBaseRenderInv=renderInv;renderInv=function(){dwxBaseRenderInv();for(const row of rowPool){const id=row._sell?.dataset.id;if(!id||row.style.display==="none")continue;let p=row.querySelector(".dwx-protect");if(!p){p=document.createElement("button");p.type="button";p.className="btn dwx-protect";p.addEventListener("click",e=>{e.stopPropagation();const it=loot.find(x=>x.id===p.dataset.id);if(it){dwxProtect(it.id,!dwxIsProtected(it));renderInv()}});row.querySelector(".actions")?.prepend(p)}const it=loot.find(x=>x.id===id);p.dataset.id=id;p.textContent=dwxIsProtected(it)?"🔒":"🔓";p.classList.toggle("on",dwxIsProtected(it));if(it?.relicId){row._rar.textContent="✦ RELIC";row._rar.className="rar legendary"}}};
(function dwxInventoryControls(){const filters=UI.invSearch.closest(".filters");if(!filters)return;const r=document.createElement("div");r.className="dwx-inv-modes";r.innerHTML=`<button class="dwx-chip on" data-im="all">All</button><button class="dwx-chip" data-im="needed">Equipment Needed</button><button class="dwx-chip" data-im="upgrades">Upgrades</button><button class="dwx-chip" data-im="relics">Relics</button><button class="dwx-chip" data-im="legendary">Legendary+</button><button class="dwx-chip" data-im="protected">Protected</button>`;filters.after(r);r.addEventListener("click",e=>{const b=e.target.closest("[data-im]");if(!b)return;dwxInvMode=b.dataset.im;r.querySelectorAll("[data-im]").forEach(x=>x.classList.toggle("on",x===b));markUI("inv")})})();

function dwxPlacement(type,x,y){const def=DWX_BUILDINGS[type];if(!def||!dwxUnlocked(type))return{quality:"INVALID",reason:"This structure is not unlocked yet."};if(!inBounds(x,y)||!tSeen[idx(x,y)]||!isWalkable(tType[idx(x,y)]))return{quality:"INVALID",reason:"Choose explored clear tunnel space."};if(Math.hypot(x-fortress.x,y-fortress.y)<2||forge&&forge.x===x&&forge.y===y||depots.some(b=>b.x===x&&b.y===y)||towers.some(b=>b.x===x&&b.y===y)||dwx.buildings.some(b=>b.x===x&&b.y===y))return{quality:"INVALID",reason:"That tile is occupied."};if(type==="lift_shaft"){const open=dwxBuildings("lift_shaft").find(b=>!b.pairId);if(open&&Math.hypot(open.x-x,open.y-y)<40)return{quality:"POOR",reason:"A lift pair this close saves little travel."}}const dwarvesNear=dwarves.filter(d=>dist2(d.x,d.y,x,y)<=def.range*def.range).length,threats=enemies.filter(e=>dist2(e.x,e.y,x,y)<=Math.max(10,def.range)**2).length+nspawnersNear(x,y,Math.max(14,def.range+5)),same=dwxBuildings(type).reduce((m,b)=>Math.min(m,Math.hypot(b.x-x,b.y-y)),Infinity);let score=28+dwarvesNear*7;if(type==="barracks"||type==="infirmary")score+=threats*12;if(type==="survey_hall")score+=Math.min(35,depthAt(x,y)/12);let reason=`${dwarvesNear} dwarves in coverage`;if(threats)reason+=` • ${threats} nearby threats`;if(same<def.range*1.5){score-=24;reason+=" • overlaps existing coverage"}if(type==="lift_shaft"){const open=dwxBuildings("lift_shaft").find(b=>!b.pairId);if(open){const saving=Math.round(Math.hypot(open.x-x,open.y-y));score+=Math.min(48,saving/5);reason=`Estimated trip reduction: ${saving} tiles`}}return{quality:score>=70?"EXCELLENT":score>=46?"GOOD":"POOR",reason,score}}
function nspawnersNear(x,y,r){return spawners.filter(s=>dist2(s.x,s.y,x,y)<=r*r).length}
function dwxPlace(type,x,y){const p=dwxPlacement(type,x,y),cost=dwxBuildingCost(type);if(p.quality==="INVALID"){toast(p.reason);return false}if(gold<cost){toast("Not enough gold.");return false}gold-=cost;const b={id:uuid(),type,x,y,level:1,hp:320,maxHp:320,stats:{serviced:0,goldReturned:0},builtAt:Date.now()};if(type==="lift_shaft"){const mate=dwxBuildings("lift_shaft").find(v=>!v.pairId);if(mate){b.pairId=mate.id;mate.pairId=b.id}}dwx.buildings.push(b);dwx.latestDiscovery={kind:"building",id:b.id,x,y};dwxBuildType=null;dwxRefreshBuild();markUI("all");toast(`${DWX_BUILDINGS[type].name} built — ${p.quality}.`);return true}
const dwxBaseCanBuildAt=canBuildAt;canBuildAt=function(x,y){if(dwx.buildings.some(b=>b.x===x&&b.y===y))return false;return dwxBaseCanBuildAt(x,y)};
(function dwxBuildControls(){const section=UI.tabBuild;if(!section)return;const c=document.createElement("div");c.className="card";c.innerHTML=`<div class="row" style="justify-content:space-between"><b>Fortress Civic Works</b><span class="pill" id="dwxUnlockDepth">Depth 0</span></div><div class="tiny" style="margin:6px 0 8px">Build strategic infrastructure; dwarves remain autonomous.</div><div id="dwxBuildGrid" class="dwx-buildgrid"></div>`;section.appendChild(c)})();
function dwxRefreshBuild(){const grid=document.getElementById("dwxBuildGrid");if(!grid)return;document.getElementById("dwxUnlockDepth").textContent=`Depth ${Math.round(dwxDeepest())}`;grid.innerHTML="";for(const[type,d]of Object.entries(DWX_BUILDINGS)){const u=dwxUnlocked(type),b=document.createElement("button");b.type="button";b.dataset.type=type;b.disabled=!u;b.className="dwx-build"+(u?"":" locked")+(dwxBuildType===type?" on":"");b.innerHTML=`<b>${d.icon} ${d.name} — ${dwxBuildingCost(type)}g</b><span>${u?d.desc:`Unlock: ${d.depth?`Depth ${d.depth}`:"Great Hall"}`}</span>`;grid.appendChild(b)}grid.onclick=e=>{const b=e.target.closest("[data-type]");if(!b||b.disabled)return;dwxBuildType=dwxBuildType===b.dataset.type?null:b.dataset.type;setBuildMode(BUILD_NONE);dwxRefreshBuild();toast(dwxBuildType?`Place ${DWX_BUILDINGS[dwxBuildType].name}.`:"Civic build mode off.")}}
dwxRefreshBuild();

(function dwxPlacementInfo(){const p=document.createElement("div");p.id="dwxPlaceInfo";document.body.appendChild(p)})();
function dwxUpdatePlaceInfo(){const p=document.getElementById("dwxPlaceInfo");if(!p)return;if(!dwxBuildType){p.style.display="none";return}const x=Math.round(mouseWorld.x),y=Math.round(mouseWorld.y),d=DWX_BUILDINGS[dwxBuildType],q=dwxPlacement(dwxBuildType,x,y);p.style.display="block";p.innerHTML=`<b>${d.icon} ${d.name}</b> · Cost ${dwxBuildingCost(dwxBuildType)}g<br>${d.desc}<br>Range: ${d.range} tiles · Placement: <span class="dwx-q ${q.quality.toLowerCase()}">${q.quality}</span><br><span class="tiny">${q.reason}</span>`}
window.addEventListener("mousemove",dwxUpdatePlaceInfo);
canvas.addEventListener("mousedown",e=>{if(e.button!==0)return;const r=canvas.getBoundingClientRect(),w=screenToWorld(e.clientX-r.left,e.clientY-r.top),x=Math.round(w.x),y=Math.round(w.y);if(dwxBuildType){e.preventDefault();e.stopImmediatePropagation();dwxPlace(dwxBuildType,x,y);return}const b=dwx.buildings.find(v=>Math.hypot(v.x-w.x,v.y-w.y)<1);if(b){e.preventDefault();e.stopImmediatePropagation();dwxShowBuilding(b)}},true);
function dwxShowBuilding(b){const d=DWX_BUILDINGS[b.type],p=document.getElementById("dwxPlaceInfo");if(!d||!p)return;cam.x=b.x;cam.y=b.y;p.style.display="block";p.innerHTML=`<b>${d.icon} ${d.name} · Lv ${b.level}</b><br>${d.desc}<br>HP ${Math.round(b.hp||b.maxHp)}/${b.maxHp} · Depth ${depthAt(b.x,b.y)}<br><span class="tiny">Serviced ${(b.stats?.serviced||0)} dwarves • Bonus gold ${(b.stats?.goldReturned||0)}</span>`;clearTimeout(p._hide);p._hide=setTimeout(()=>{if(!dwxBuildType)p.style.display="none"},4200)}

function dwxDrawBuildings(){for(const b of dwx.buildings){const d=DWX_BUILDINGS[b.type],p=worldToScreen(b.x,b.y),s=p.s;if(!d||p.x<-s||p.y<-s||p.x>innerWidth+s||p.y>innerHeight+s)continue;ctx.save();ctx.shadowColor=DWX_BUILD_COL[b.type]||"#fff";ctx.shadowBlur=Math.max(2,s*.28);ctx.fillStyle="rgba(23,22,20,.97)";ctx.fillRect(p.x+s*.12,p.y+s*.18,s*.76,s*.64);ctx.shadowBlur=0;ctx.strokeStyle=DWX_BUILD_COL[b.type]||"#fff";ctx.lineWidth=Math.max(1,s*.05);ctx.strokeRect(p.x+s*.12,p.y+s*.18,s*.76,s*.64);ctx.font=`${Math.max(10,s*.50)}px system-ui`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(d.icon,p.x+s*.5,p.y+s*.49);ctx.restore()}if(dwxBuildType){const x=Math.round(mouseWorld.x),y=Math.round(mouseWorld.y);if(inBounds(x,y)){const d=DWX_BUILDINGS[dwxBuildType],q=dwxPlacement(dwxBuildType,x,y),p=worldToScreen(x,y),s=p.s;ctx.save();ctx.globalAlpha=.72;ctx.fillStyle=q.quality==="INVALID"?"rgba(255,70,90,.36)":"rgba(120,220,255,.30)";ctx.fillRect(p.x,p.y,s,s);ctx.strokeStyle=DWX_BUILD_COL[dwxBuildType]||"#78dcff";ctx.lineWidth=2;ctx.beginPath();ctx.arc(p.x+s*.5,p.y+s*.5,d.range*s,0,Math.PI*2);ctx.stroke();ctx.restore()}}}
const dwxBaseDraw=draw;draw=function(){dwxBaseDraw();dwxDrawBuildings()};

(function dwxMap(){const box=document.createElement("aside");box.id="dwxMap";box.className="closed";box.innerHTML=`<div class="dwx-maphead"><span>🗺 Underground Map</span><button id="dwxMapToggle" class="btn icon" type="button">▾</button></div><div class="dwx-mapbody"><div class="dwx-mapwrap"><canvas id="dwxMini" width="216" height="190"></canvas></div><div class="dwx-mapnav"><button data-nav="fortress">Fortress</button><button data-nav="selected">Selected Dwarf</button><button data-nav="deepest">Deepest Explored</button><button data-nav="latest">Latest Discovery</button></div></div>`;document.body.appendChild(box);const hud=document.createElement("button");hud.className="btn";hud.textContent="🗺 Map";UI.btnFocus.parentElement.insertBefore(hud,UI.btnFocus);hud.addEventListener("click",()=>dwxToggleMap(true));box.querySelector("#dwxMapToggle").addEventListener("click",()=>dwxToggleMap());box.querySelector(".dwx-mapnav").addEventListener("click",e=>{const b=e.target.closest("[data-nav]");if(b)dwxMapNav(b.dataset.nav)});box.querySelector("#dwxMini").addEventListener("click",e=>{const r=e.currentTarget.getBoundingClientRect();cam.x=clamp((e.clientX-r.left)/r.width*W,0,W-1);cam.y=clamp((e.clientY-r.top)/r.height*H,0,H-1)});if(UI.mobileBar){const m=document.createElement("button");m.className="mbtn";m.type="button";m.innerHTML="<b>🗺</b><span>Map</span>";UI.mobileBar.insertBefore(m,UI.mFocus);m.addEventListener("click",()=>dwxToggleMap(true))}})();
function dwxToggleMap(force){dwxMapOpen=force===true?true:!dwxMapOpen;document.getElementById("dwxMap")?.classList.toggle("closed",!dwxMapOpen);if(dwxMapOpen)dwxDrawMap(true)}
function dwxMapNav(k){if(k==="fortress"){cam.x=fortress.x;cam.y=fortress.y}else if(k==="selected"){const d=getSelectedDwarf();if(d){cam.x=d.x;cam.y=d.y}}else if(k==="deepest"){const d=dwarves.slice().sort((a,b)=>depthAt(b.x,b.y)-depthAt(a.x,a.y))[0];if(d){cam.x=d.x;cam.y=d.y}}else if(k==="latest"&&dwx.latestDiscovery){cam.x=dwx.latestDiscovery.x;cam.y=dwx.latestDiscovery.y}if(isMobileLayout())dwxToggleMap(false)}
function dwxDrawMap(force=false){const c=document.getElementById("dwxMini");if(!c||!dwxMapOpen&&!force)return;const now=performance.now();if(!force&&now-dwxLastMap<850)return;dwxLastMap=now;const g=c.getContext("2d"),w=c.width,h=c.height;g.fillStyle="#030507";g.fillRect(0,0,w,h);const step=isMobileLayout()?10:8;for(let y=0;y<H;y+=step)for(let x=0;x<W;x+=step){let seen=false,open=false,ore=0;for(let yy=y;yy<Math.min(H,y+step)&&!seen;yy+=2)for(let xx=x;xx<Math.min(W,x+step);xx+=2){const i=idx(xx,yy);if(tSeen[i]){seen=true;open=open||isWalkable(tType[i]);if(tType[i]>=TILE_GOLD&&tType[i]<=TILE_DIAMOND)ore=Math.max(ore,tType[i])}}if(!seen)continue;g.fillStyle=open?"#25333c":"#11171d";g.fillRect(x/W*w,y/H*h,Math.max(1,step/W*w),Math.max(1,step/H*h));if(ore){g.fillStyle=ore===TILE_DIAMOND?"#d8f2ff":ore===TILE_EMERALD?"#7be0a4":ore===TILE_SAPPHIRE?"#78dcff":"#d7ab54";g.fillRect(x/W*w,y/H*h,2,2)}}const dot=(x,y,col,r=2)=>{g.fillStyle=col;g.beginPath();g.arc(x/W*w,y/H*h,r,0,Math.PI*2);g.fill()};dot(fortress.x,fortress.y,"#ffd66f",3);for(const b of dwx.buildings)dot(b.x,b.y,DWX_BUILD_COL[b.type]||"#fff",2.4);for(const b of depots)dot(b.x,b.y,"#aab7c4",2);for(const t of towers)dot(t.x,t.y,"#ffaf68",2);for(const d of dwarves)dot(d.x,d.y,selected.kind==="dwarf"&&selected.id===d.id?"#fff":"#7be0a4",selected.kind==="dwarf"&&selected.id===d.id?3:1.5);for(const s of spawners){const x=Math.round(s.x),y=Math.round(s.y);if(inBounds(x,y)&&tSeen[idx(x,y)])dot(s.x,s.y,"#d66cff",2.4)}for(const e of enemies){const x=Math.round(e.x),y=Math.round(e.y);if((e.maxHp||0)>120&&inBounds(x,y)&&tSeen[idx(x,y)])dot(e.x,e.y,"#ff6f79",2.2)}for(const s of dwx.surveys.filter(s=>s.expires>nowSec())){g.strokeStyle=s.kind==="danger"?"#ff6f79":"#78dcff";g.globalAlpha=.55;g.beginPath();g.arc(s.x/W*w,s.y/H*h,Math.max(4,s.radius/W*w),0,Math.PI*2);g.stroke();g.globalAlpha=1}g.strokeStyle="#fff";g.strokeRect(cam.x/W*w-5,cam.y/H*h-4,10,8)}

function dwxSafeTileNear(x,y,r=14){let best=null,bd=Infinity;for(let yy=Math.max(0,y-r);yy<=Math.min(H-1,y+r);yy++)for(let xx=Math.max(0,x-r);xx<=Math.min(W-1,x+r);xx++){const i=idx(xx,yy);if(!tSeen[i]||!isWalkable(tType[i])||enemies.some(e=>dist2(e.x,e.y,xx,yy)<36))continue;const d=dist2(x,y,xx,yy);if(d<bd){bd=d;best={x:xx,y:yy}}}return best}
const dwxBaseStats=applyDwarfStats;applyDwarfStats=function(d){dwxBaseStats(d);const dep=depthAt(Math.round(d.x),Math.round(d.y));if(d.pickaxe?.relicId==="heart_of_mountain")d.miningPower=Math.round(d.miningPower*(1+Math.min(.75,dep/1000)));if(d.weapon?.relicId==="king_under_stone")d.dmg=Math.round(d.dmg*(1+Math.min(.80,(d.level||1)*.035)));if(d.weapon?.relicId==="deepward_bow")d.dmg=Math.round(d.dmg*(1+Math.min(.55,dep/900)));if(d.weapon?.relicId==="spawnbreaker"&&spawners.some(s=>dist2(s.x,s.y,d.x,d.y)<225))d.dmg=Math.round(d.dmg*1.65);if(d.armor?.relicId==="grudgeplate")d.defense=Math.min(.78,d.defense+(d.dwxGrudges||0)*.006);if(d.armor?.relicId==="armor_deep_road")d.speed*=1+Math.min(.28,dep/1500);if((d.dwxInspiredUntil||0)>nowSec()){d.dmg=Math.round(d.dmg*1.10);d.miningPower=Math.round(d.miningPower*1.10);d.speed*=1.08}if((d.dwxRationUntil||0)>nowSec()){d.miningRate*=1.12;d.speed*=1.08}};
const dwxBaseDwarf=updateDwarf;updateDwarf=function(d,dt){const xp0=d.xp||0,g0=gold;applyDwarfStats(d);const b=dwxNearest("barracks",d.x,d.y);if(b&&b.distance<=DWX_BUILDINGS.barracks.range){d.dmg=Math.round(d.dmg*1.08);d.defense=Math.min(.8,d.defense+.05);b.building.stats.serviced++}const inf=dwxNearest("infirmary",d.x,d.y);if(inf&&inf.distance<=DWX_BUILDINGS.infirmary.range&&d.hp<d.maxHp){d.hp=Math.min(d.maxHp,d.hp+dt*9);inf.building.stats.serviced++}const tav=dwxNearest("tavern",d.x,d.y);if(tav&&tav.distance<=DWX_BUILDINGS.tavern.range&&d.hp<d.maxHp)d.hp=Math.min(d.maxHp,d.hp+dt*3.5);const sh=dwxNearest("shrine",d.x,d.y);if(sh&&sh.distance<=DWX_BUILDINGS.shrine.range&&(d.level||1)>=6&&Math.random()<dt*.05)d.dwxInspiredUntil=nowSec()+40;if(d.armor?.relicId==="voidhook"&&d.hp<d.maxHp*.16&&(d.dwxVoidhookCd||0)<nowSec()){const safe=dwxSafeTileNear(fortress.x,fortress.y,18);if(safe){d.x=safe.x;d.y=safe.y;d.path=null;d.dwxVoidhookCd=nowSec()+90;fxBurst(d.x,d.y,"rgba(190,130,255,.95)",1.8)}}const lifts=dwxBuildings("lift_shaft").filter(x=>x.pairId);if(d.path?.length>70&&lifts.length){const goal=d.path[d.path.length-1],entry=lifts.find(x=>dist2(x.x,x.y,d.x,d.y)<5);if(entry&&(d.dwxLiftCd||0)<nowSec()){const exit=dwx.buildings.find(x=>x.id===entry.pairId);if(exit&&Math.hypot(exit.x-goal.x,exit.y-goal.y)+20<Math.hypot(d.x-goal.x,d.y-goal.y)){d.x=exit.x;d.y=exit.y;d.path=bfsPath(Math.round(d.x),Math.round(d.y),(x,y)=>x===goal.x&&y===goal.y,90000)||d.path;d.dwxLiftCd=nowSec()+5;fxText(d.x,d.y,"LIFT","rgba(180,220,255,.95)")}}}dwxBaseDwarf(d,dt);if(gold>g0){const bonus=Math.round((gold-g0)*dwxTreasuryBonus());if(bonus>0){gold+=bonus;dwx.stats.goldBonus=(dwx.stats.goldBonus||0)+bonus;const tr=dwxNearest("treasury",d.x,d.y);if(tr)tr.building.stats.goldReturned+=bonus}}const gain=(d.xp||0)-xp0;if(gain>0){let bonus=0;if(d.armor?.relicId==="ancestors_token")bonus+=Math.ceil(gain*.15);if((d.level||1)>=6&&dwxCount("shrine"))bonus+=Math.ceil(gain*.04);d.xp+=bonus}applyDwarfStats(d)};
const dwxBaseVisibility=refreshVisibility;refreshVisibility=function(dt=0){dwxBaseVisibility(dt);for(const d of dwarves)if(d.armor?.relicId==="lantern_first_delver")stampVision(Math.round(d.x),Math.round(d.y),DW_VIS_R+8)};
const dwxBaseDead=removeDead;removeDead=function(){for(const d of dwarves){if(d.hp>0)continue;if(d.armor?.relicId==="last_kings_bulwark"&&(d.dwxBulwarkCd||0)<nowSec()){d.hp=1;d.dwxBulwarkCd=nowSec()+150;const safe=dwxSafeTileNear(fortress.x,fortress.y,16);if(safe){d.x=safe.x;d.y=safe.y;d.path=null}fxText(d.x,d.y,"LAST STAND","rgba(255,214,111,.98)",1.2);continue}const inf=dwxNearest("infirmary",d.x,d.y);if(inf&&(inf.building.dwxRescueCd||0)<nowSec()){d.hp=1;inf.building.dwxRescueCd=nowSec()+90;d.x=inf.building.x;d.y=inf.building.y;d.path=null;fxText(d.x,d.y,"RESCUED","rgba(120,255,170,.98)",1.2)}}return dwxBaseDead()};

const dwxBaseMineTile=mineTile;mineTile=function(tx,ty,power){const result=dwxBaseMineTile(tx,ty,power);if(!result?.destroyed)return result;const miner=dwarves.find(d=>d.pickaxe?.relicId&&dist2(d.x,d.y,tx,ty)<=6);if(!miner)return result;if(miner.pickaxe.relicId==="fortunes_end"){if(result.currency)result.currency.value=Math.round(result.currency.value*1.35);else if(Math.random()<.055)result.currency={kind:"currency",currency:Math.random()<.7?"sapphire":"emerald",value:Math.round(120+depthAt(tx,ty)*.18)}}if(miner.pickaxe.relicId==="worldsplitter"&&Math.random()<.18&&!miner.dwxWorldsplitterBusy){miner.dwxWorldsplitterBusy=true;try{for(let oy=-1;oy<=1;oy++)for(let ox=-1;ox<=1;ox++){if(!ox&&!oy)continue;const x=tx+ox,y=ty+oy;if(!inBounds(x,y)||!isDiggable(tType[idx(x,y)]))continue;const r=dwxBaseMineTile(x,y,Math.max(1,power*.28));if(r?.currency)groundLoot.push({id:uuid(),x,y,payload:r.currency})}}finally{miner.dwxWorldsplitterBusy=false}}return result};

function dwxMakeRelic(def){const x={id:uuid(),name:def.name,category:def.category,rarity:"legendary",relicId:def.id,relic:true,protected:true,flavor:def.flavor,relicEffect:def.effect,effectName:"Relic",effectDesc:def.effect,sell:2500,identityDone:true};if(def.category==="weapon")Object.assign(x,{weaponType:def.weaponType||"axe",...def.stats});else if(def.category==="armor")Object.assign(x,{armorTier:def.armorTier||"titanium",...def.stats});else Object.assign(x,def.stats);return x}
const dwxBaseEnemyDeath=handleEnemyDeath;handleEnemyDeath=function(e){const killer=e?.lastHit?.kind==="dwarf"?dwarves.find(d=>d.id===e.lastHit.id):null;if(killer?.armor?.relicId==="grudgeplate"&&(e.maxHp||0)>110)killer.dwxGrudges=Math.min(30,(killer.dwxGrudges||0)+1);dwxBaseEnemyDeath(e);const dep=e?.sourceDepth||0;if(dep<220||Math.random()>Math.min(.012,.0012+dep/180000+dwxVaultBonus()))return;const pool=DWX_RELICS.filter(r=>!dwx.relicsFound.includes(r.id));if(!pool.length)return;const def=pickRand(pool),it=dwxMakeRelic(def);dwx.relicsFound.push(def.id);dwxProtect(it.id,true);dwx.stats.relicDrops=(dwx.stats.relicDrops||0)+1;groundLoot.push({id:uuid(),x:Math.round(e.x),y:Math.round(e.y),payload:{kind:"item",item:it}});dwx.latestDiscovery={kind:"relic",id:def.id,x:Math.round(e.x),y:Math.round(e.y)};fxBurst(e.x,e.y,"rgba(255,214,111,.95)",2.2)};
(function dwxRelicModal(){const m=document.createElement("div");m.id="dwxRelicModal";m.innerHTML=`<div class="dwx-reliccard"><div class="dwx-relictag">✦ RELIC DISCOVERED ✦</div><div class="dwx-relicname"></div><div class="dwx-relicflavor"></div><div class="dwx-relicstats"></div><div class="dwx-relicactions"><button data-ra="equip" class="btn primary">Equip</button><button data-ra="vault" class="btn">Send to Vault</button><button data-ra="continue" class="btn">Continue</button></div></div>`;document.body.appendChild(m);m.addEventListener("click",e=>{const b=e.target.closest("[data-ra]");if(!b)return;const it=loot.find(x=>x.id===m.dataset.itemId);if(b.dataset.ra==="equip"&&it){const d=getSelectedDwarf();if(!d){toast("Select a dwarf first.");return}if(d[it.category]){toast("That equipment slot is occupied. Relic remains protected.");return}const i=loot.indexOf(it);if(i>=0)loot.splice(i,1);d[it.category]=it;applyDwarfStats(d);markUI("all")}if(b.dataset.ra==="vault"&&it)dwxProtect(it.id,true);m.classList.remove("open")})})();
function dwxPresentRelic(it){const m=document.getElementById("dwxRelicModal");if(!m)return;m.dataset.itemId=it.id;m.querySelector(".dwx-relicname").textContent=it.name;m.querySelector(".dwx-relicflavor").textContent=`“${it.flavor||"An old power returns to the Deep."}”`;const stats=it.category==="weapon"?`Ancient Weapon • Damage ${it.dmg} • ${it.ranged?`Range ${it.range}`:"Melee"}`:it.category==="pickaxe"?`Ancient Pickaxe • Mining ${it.miningPower} • Speed x${Number(it.miningRate||1).toFixed(2)}`:`Ancient Armor • Defense ${Math.round((it.defense||0)*100)}% • +${it.bonusHp||0} HP`;m.querySelector(".dwx-relicstats").innerHTML=`${stats}<br><b>Relic Effect:</b> ${it.relicEffect||it.effectDesc||"Unknown"}`;m.classList.add("open")}
function dwxCheckRelics(){for(const it of loot){if(!it.relicId||dwx.presentedRelics.includes(it.relicId))continue;dwx.presentedRelics.push(it.relicId);dwxPresentRelic(it);break}}

function dwxSurvey(now){if(now-dwxLastSurvey<24||!dwxCount("survey_hall"))return;dwxLastSurvey=now;for(const hall of dwxBuildings("survey_hall")){const r=72+hall.level*30;let best=null,bv=-1;for(let n=0;n<260;n++){const a=Math.random()*Math.PI*2,rr=24+Math.random()*r,x=Math.round(hall.x+Math.cos(a)*rr),y=Math.round(hall.y+Math.sin(a)*rr);if(!inBounds(x,y)||tSeen[idx(x,y)])continue;const tt=tType[idx(x,y)];let v=0;if(tt===TILE_DIAMOND)v=5;else if(tt===TILE_EMERALD)v=4;else if(tt===TILE_SAPPHIRE)v=3;else if(tt===TILE_GOLD)v=2;else if(tt===TILE_SPAWNER_SEALED)v=2.5;if(v>bv){bv=v;best={x,y,kind:tt===TILE_SPAWNER_SEALED?"danger":"resource"}}}if(best){best.expires=now+75;best.radius=Math.max(14,28-hall.level*4);dwx.surveys.push(best);dwx.surveys=dwx.surveys.filter(s=>s.expires>now).slice(-8);dwx.latestDiscovery={kind:"survey",x:best.x,y:best.y};toast(best.kind==="danger"?"Surveyors report suspicious Deepwild activity beyond the explored frontier.":"Surveyors report a valuable formation beyond the explored frontier.")}}}
function dwxCivicTick(){dwx.maxDepth=Math.max(dwx.maxDepth||0,...dwarves.map(d=>depthAt(Math.round(d.x),Math.round(d.y))));const now=nowSec();if(now-dwxLastCivic<.8)return;dwxLastCivic=now;for(const farm of dwxBuildings("mushroom_farm"))for(const d of dwarves)if(dist2(d.x,d.y,farm.x,farm.y)<=DWX_BUILDINGS.mushroom_farm.range**2&&Math.random()<.12)d.dwxRationUntil=now+28;if(dwxCount("tavern")&&!dwx.visitorReady&&now>(dwx.visitorAt||0)+120){dwx.visitorReady=true;dwx.visitorAt=now;toast("🍺 A visiting dwarf has arrived at the Tavern. Your next recruit is discounted.")}dwxSurvey(now);dwxCheckRelics();dwxRefreshBuild()}
const dwxBaseCombat=updateSpawnersAndCombat;updateSpawnersAndCombat=function(dt){dwxBaseCombat(dt);dwxCivicTick()};
const dwxBaseRecruitCost=recruitCost;recruitCost=function(){let c=dwxBaseRecruitCost();if(dwxCount("great_hall"))c*=1-Math.min(.10,dwxBuildings("great_hall").reduce((n,b)=>n+(b.level>=3?.10:b.level===2?.05:0),0));if(dwx.visitorReady)c*=.82;return Math.max(1,Math.round(c))};
UI.btnRecruit.addEventListener("click",e=>{if(dwarves.length>=dwxPopCap()){e.stopImmediatePropagation();toast(`Great Hall population capacity reached (${dwxPopCap()}).`);return}if(dwx.visitorReady)setTimeout(()=>dwx.visitorReady=false,0)},true);
const dwxBaseCraftCost=forgeCraftCost,dwxBaseForgeUpgrade=forgeUpgradeCost;forgeCraftCost=function(l){return Math.max(1,Math.round(dwxBaseCraftCost(l)*(1-dwxWorkshopDiscount())))};forgeUpgradeCost=function(l){return Math.max(1,Math.round(dwxBaseForgeUpgrade(l)*(1-dwxWorkshopDiscount())))};

// Save expansion fields inside the same compressed payload used by Firebase/local saves.
const dwxBaseSaveAsync=saveGameAsync;saveGameAsync=async function(){const realToast=toast;let savedToast="Saved locally.";toast=function(m){if(/^Saved locally/i.test(String(m)))savedToast=m;else realToast(m)};try{await dwxBaseSaveAsync();const raw=localStorage.getItem(SAVE_KEY);if(raw){let json=raw;if(raw.startsWith(SAVE_MAGIC))json=await gunzipB64ToStr(raw.slice(SAVE_MAGIC.length));const save=JSON.parse(json);save.fortressExpansion=dwx;localStorage.setItem(SAVE_KEY,SAVE_MAGIC+await gzipStrToB64(JSON.stringify(save)))}realToast(savedToast)}catch(err){console.error(err);realToast("Save failed (fortress expansion).")}finally{toast=realToast}};
const dwxBaseLoadAsync=loadGameAsync;loadGameAsync=async function(){const realToast=toast;let loadedToast="Loaded local save.";toast=function(m){if(/Loaded local save/i.test(String(m)))loadedToast=m;else realToast(m)};try{const raw=localStorage.getItem(SAVE_KEY);let ext=null;if(raw){let json=raw;if(raw.startsWith(SAVE_MAGIC))json=await gunzipB64ToStr(raw.slice(SAVE_MAGIC.length));ext=JSON.parse(json)?.fortressExpansion||null}await dwxBaseLoadAsync();dwx=Object.assign(dwxFresh(),ext||{});for(const k of["buildings","protectedIds","relicsFound","presentedRelics","surveys"])if(!Array.isArray(dwx[k]))dwx[k]=[];for(const it of loot)if(it.relicId)dwxProtect(it.id,true);dwxRefreshBuild();markUI("all");realToast(loadedToast)}catch(err){console.error(err);realToast("Load failed (fortress expansion).")}finally{toast=realToast}};

const dwxBaseFlush=flushUI;flushUI=function(nowMs){dwxBaseFlush(nowMs);dwxUpdatePlaceInfo();dwxDrawMap(false)};
markUI("all");
toast("Fortress Command expansion loaded — roster, safe auto-equip, minimap, civic works and relics online.");
