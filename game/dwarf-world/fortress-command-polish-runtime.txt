// Fortress Command progression + placement polish. Runs after the base DWX runtime.

function dwxUpgradeCost(b){const d=DWX_BUILDINGS[b.type];return Math.max(1,Math.round(d.cost*(.55+b.level*.42)*(1-dwxWorkshopDiscount())))}
function dwxUpgradeBuilding(id){const b=dwx.buildings.find(x=>x.id===id);if(!b)return;if(b.level>=3){toast(`${DWX_BUILDINGS[b.type].name} is already max level.`);return}const cost=dwxUpgradeCost(b);if(gold<cost){toast(`Need ${cost}g to upgrade.`);return}gold-=cost;b.level++;b.maxHp=Math.round((b.maxHp||320)*1.25);b.hp=b.maxHp;dwxRefreshBuild();dwxOpenBuildingPanel(b);markUI("all");toast(`${DWX_BUILDINGS[b.type].name} upgraded to Level ${b.level}.`)}

(function dwxBuildingPanelInstall(){
 const p=document.createElement("section");p.id="dwxBuildingPanel";p.style.cssText="position:fixed;z-index:62;right:12px;bottom:74px;width:min(330px,calc(100vw - 24px));display:none;padding:12px;border:1px solid rgba(255,255,255,.16);border-radius:13px;background:rgba(8,11,15,.95);box-shadow:0 18px 48px rgba(0,0,0,.48);backdrop-filter:blur(10px)";p.innerHTML=`<div style="display:flex;justify-content:space-between;gap:8px"><div><b id="dwxBuildingTitle"></b><div id="dwxBuildingDesc" class="tiny" style="margin-top:3px"></div></div><button id="dwxBuildingClose" class="btn icon" type="button">×</button></div><div id="dwxBuildingStats" style="font-size:10px;line-height:1.55;margin:9px 0"></div><div style="display:flex;gap:6px"><button id="dwxBuildingUpgrade" class="btn primary" type="button">Upgrade</button><button id="dwxBuildingFocus" class="btn" type="button">Focus</button></div>`;document.body.appendChild(p);p.querySelector("#dwxBuildingClose").onclick=()=>p.style.display="none";p.querySelector("#dwxBuildingUpgrade").onclick=()=>dwxUpgradeBuilding(p.dataset.id);p.querySelector("#dwxBuildingFocus").onclick=()=>{const b=dwx.buildings.find(x=>x.id===p.dataset.id);if(b){cam.x=b.x;cam.y=b.y}}
})();
function dwxBuildingEffectText(b){const l=b.level||1;if(b.type==="great_hall")return `Population capacity +${l>=3?16:l===2?8:3}${l>=2?` • Recruitment discount ${l>=3?10:5}%`:""}`;if(b.type==="tavern")return `Recovery aura • Visitor quality ${l===1?"standard":l===2?"improved":"elite"}`;if(b.type==="treasury")return `Returned treasure bonus ${l===1?3:l===2?6:10}%`;if(b.type==="barracks")return `Combat support radius ${DWX_BUILDINGS[b.type].range} • +8% damage • +5% defense`;if(b.type==="workshop")return `Forge/structure efficiency ${l===1?"3%":l===2?"6% / 3%":"10% / 5%"}`;if(b.type==="mushroom_farm")return `Deepcap ration support • Level ${l} production`;if(b.type==="infirmary")return `Strong recovery • emergency rescue cooldown ${l===3?"improved":"active"}`;if(b.type==="survey_hall")return `Survey range ${72+l*30} • accuracy improves with level`;if(b.type==="lift_shaft")return b.pairId?"Connected lift network":"Awaiting paired Lift Shaft";if(b.type==="shrine")return `Veteran Inspired buff • bonus XP`;if(b.type==="great_vault")return `Legendary protection • relic discovery bonus`;return""}
function dwxOpenBuildingPanel(b){const p=document.getElementById("dwxBuildingPanel"),d=DWX_BUILDINGS[b.type];if(!p||!d)return;p.dataset.id=b.id;p.style.display="block";p.querySelector("#dwxBuildingTitle").textContent=`${d.icon} ${d.name} · Lv ${b.level}`;p.querySelector("#dwxBuildingDesc").textContent=d.desc;p.querySelector("#dwxBuildingStats").innerHTML=`${dwxBuildingEffectText(b)}<br>HP ${Math.round(b.hp||b.maxHp)}/${b.maxHp} • Depth ${depthAt(b.x,b.y)}<br>Serviced ${(b.stats?.serviced||0)} dwarves • Bonus gold ${(b.stats?.goldReturned||0)}`;const u=p.querySelector("#dwxBuildingUpgrade");u.disabled=b.level>=3;u.textContent=b.level>=3?"Max Level":`Upgrade (${dwxUpgradeCost(b)}g)`}
const dwxPreviousShowBuilding=dwxShowBuilding;dwxShowBuilding=function(b){dwxPreviousShowBuilding(b);dwxOpenBuildingPanel(b)};

(function dwxVaultActions(){
 const modes=document.querySelector(".dwx-inv-modes");if(!modes)return;const protect=document.createElement("button");protect.id="dwxProtectLegendary";protect.className="dwx-chip";protect.textContent="🔐 Protect Legendary+";protect.type="button";protect.onclick=()=>{let n=0;for(const it of loot){if(it.relicId||it.rarity==="legendary"){if(!dwxIsProtected(it))n++;dwxProtect(it.id,true)}}toast(`Protected ${n} Legendary/Relic item${n===1?"":"s"}.`);renderInv()};modes.appendChild(protect)
})();

// Tavern visitors become meaningfully better at higher Tavern levels without changing the normal recruit loop.
UI.btnRecruit.addEventListener("click",()=>{
 if(!dwx.visitorReady)return;const before=new Set(dwarves.map(d=>d.id)),tavernLevel=Math.max(1,...dwxBuildings("tavern").map(b=>b.level||1));setTimeout(()=>{const d=dwarves.find(x=>!before.has(x.id));if(!d)return;if(tavernLevel>=2){d.level=Math.max(d.level,2+randInt(0,2));if(!(d.perks||[]).length){const ids=Object.keys(PERKS);if(ids.length)d.perks=[pickRand(ids)]}}if(tavernLevel>=3){const cat=pickRand(["armor","weapon","pickaxe"]),rar=Math.random()<.22?"legendary":Math.random()<.58?"epic":"rare",it=cat==="armor"?mkItemArmor(rar):cat==="weapon"?mkItemWeapon(rar):mkItemPickaxe(rar);decorateItem(it,dwxDeepest());d[cat]=it}applyDwarfStats(d);d.hp=d.maxHp;markUI("all");toast(`Tavern visitor recruited: ${d.name}${tavernLevel>=3?" arrived exceptionally well equipped":""}.`)},0)
},true);

// Higher-level infirmaries shorten the rescue cooldown and higher-level farms ration more reliably.
const dwxCivicBase=dwxCivicTick;dwxCivicTick=function(){dwxCivicBase();const now=nowSec();for(const b of dwxBuildings("infirmary"))if(b.level>=3&&b.dwxRescueCd>now+60)b.dwxRescueCd=now+60;for(const farm of dwxBuildings("mushroom_farm")){const chance=farm.level>=3?.20:farm.level===2?.16:.12;for(const d of dwarves)if(dist2(d.x,d.y,farm.x,farm.y)<=DWX_BUILDINGS.mushroom_farm.range**2&&Math.random()<chance*.08)d.dwxRationUntil=now+36}}

const DWX_BASE_BUILD_META={};
DWX_BASE_BUILD_META[BUILD_DEPOT]={name:"Depot",range:12,cost:()=>COST.depot,kind:"logistics"};
DWX_BASE_BUILD_META[BUILD_ARROW]={name:"Arrow Tower",range:9.5,cost:()=>COST.arrow,kind:"defense"};
DWX_BASE_BUILD_META[BUILD_CANNON]={name:"Cannon Tower",range:8.8,cost:()=>COST.cannon,kind:"defense"};
DWX_BASE_BUILD_META[BUILD_FROST]={name:"Frost Tower",range:8.2,cost:()=>COST.frost,kind:"defense"};
DWX_BASE_BUILD_META[BUILD_HEAL]={name:"Healing Tower",range:7.5,cost:()=>COST.heal,kind:"healing"};
DWX_BASE_BUILD_META[BUILD_HASTE]={name:"Haste Tower",range:7.5,cost:()=>COST.haste,kind:"support"};
DWX_BASE_BUILD_META[BUILD_GUARD]={name:"Guard Post",range:28,cost:()=>COST.guard,kind:"defense"};
function dwxBasePlacement(mode,x,y){const m=DWX_BASE_BUILD_META[mode];if(!m)return null;if(!canBuildAt(x,y))return{quality:"INVALID",reason:"Cannot build on this tile.",meta:m};const nearD=dwarves.filter(d=>dist2(d.x,d.y,x,y)<=m.range*m.range).length,nearE=enemies.filter(e=>dist2(e.x,e.y,x,y)<=m.range*m.range).length,nearS=nspawnersNear(x,y,m.range+8);let score=30,reason="";if(m.kind==="logistics"){const nearest=Math.min(Math.hypot(x-fortress.x,y-fortress.y),...depots.map(d=>Math.hypot(x-d.x,y-d.y)));score+=nearD*10+Math.min(35,nearest/3);reason=`${nearD} active dwarves nearby • estimated travel relief ${Math.round(nearest)} tiles`}else if(m.kind==="healing"){score+=nearD*9+nearE*6;reason=`${nearD} dwarves • ${nearE} active threats in recovery coverage`}else if(m.kind==="defense"){score+=nearE*10+nearS*15+nearD*3;reason=`${nearE+nearS} threats/spawners • ${nearD} dwarves in coverage`}else{score+=nearD*7+nearE*5;reason=`${nearD} dwarves • ${nearE} threats in support radius`}return{quality:score>=72?"EXCELLENT":score>=48?"GOOD":"POOR",reason,meta:m}}
const dwxPlaceInfoBase=dwxUpdatePlaceInfo;dwxUpdatePlaceInfo=function(){if(dwxBuildType)return dwxPlaceInfoBase();const p=document.getElementById("dwxPlaceInfo"),m=DWX_BASE_BUILD_META[buildMode];if(!p)return;if(!m){p.style.display="none";return}const x=Math.round(mouseWorld.x),y=Math.round(mouseWorld.y),q=dwxBasePlacement(buildMode,x,y);p.style.display="block";p.innerHTML=`<b>${m.name}</b> · Cost ${m.cost()}g<br>Coverage radius: ${m.range} tiles · Placement: <span class="dwx-q ${q.quality.toLowerCase()}">${q.quality}</span><br><span class="tiny">${q.reason}</span>`}
const dwxDrawBuildingsBase=dwxDrawBuildings;dwxDrawBuildings=function(){dwxDrawBuildingsBase();if(dwxBuildType||buildMode===BUILD_NONE)return;const m=DWX_BASE_BUILD_META[buildMode];if(!m)return;const x=Math.round(mouseWorld.x),y=Math.round(mouseWorld.y);if(!inBounds(x,y))return;const p=worldToScreen(x,y),q=dwxBasePlacement(buildMode,x,y);ctx.save();ctx.strokeStyle=q.quality==="INVALID"?"rgba(255,90,100,.85)":"rgba(120,220,255,.72)";ctx.lineWidth=2;ctx.setLineDash([6,5]);ctx.beginPath();ctx.arc(p.x+p.s*.5,p.y+p.s*.5,m.range*p.s,0,Math.PI*2);ctx.stroke();ctx.setLineDash([]);ctx.restore()};

markUI("all");
