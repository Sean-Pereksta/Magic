// Space Tyrants — independent deep-space bases, economic warfare, limited-war
// objectives, living diplomacy, and fleet-scale visual LOD.
//
// This final runtime layer deliberately reuses the existing planet inspector,
// Fleet Command allocation modal, Rival Powers, transmissions, news, and
// strategic overlays. Deep-space facilities are real map entities, but their
// sponsoring planet remains the compatibility anchor for the older economy.

const STX_DS_VERSION=1;
const STX_DS_MAX_BASES_PER_OWNER=6;
const STX_DS_MAX_WRECKS=36;
const STX_DS_VISIBLE_TRAFFIC=5;
const STX_DS_MILITARY_INDUSTRY_UNLOCK=5;
const STX_DS_FREIGHT_TYPES=new Set(["freighter","tanker","construction","miningBarge","supply","liner","luxury","courier","research"]);
const STX_DS_BASE_TYPES={
  military:{name:"Frontier Military Base",short:"MILITARY BASE",color:"#ff786f",maxHp:150,supplyDecay:.00165,service:7,cost:{components:32,equipment:36,titanium:68,iron:54,helium:34,silicates:16}},
  trade:{name:"Deep-Space Trade Station",short:"TRADE STATION",color:"#ffd56f",maxHp:112,supplyDecay:.00135,service:10,cost:{components:30,equipment:24,iron:48,silicates:46,titanium:26,rare:14,helium:18}},
  logistics:{name:"Logistics Relay",short:"LOGISTICS RELAY",color:"#70e9ff",maxHp:122,supplyDecay:.00145,service:8,cost:{components:30,equipment:28,iron:62,titanium:36,helium:36,silicates:18}},
  sensor:{name:"Listening Outpost",short:"SENSOR OUTPOST",color:"#a98cff",maxHp:92,supplyDecay:.0011,service:11,cost:{components:28,equipment:22,titanium:34,rare:42,silicates:26,iron:18,helium:16}}
};
const STX_DS_NAME_PREFIX={
  military:["Frontier Bastion","March Citadel","Vanguard Hold","Sentinel Base","Aegis Station"],
  trade:["Meridian Exchange","Free Commerce Anchorage","Crown Exchange","Wayfarer Market","Concord Station"],
  logistics:["Meridian Relay","Longhaul Junction","Imperial Waystation","Helium Relay","Transit Nexus"],
  sensor:["Farwatch","Silent Beacon","Deepwatch Array","Horizon Listener","Vigil Outpost"]
};
let stxDSLastTick=-999,stxDSPlacement=null,stxDSAllianceGuard=false,stxDSSelectedAgreementKind=null;

function stxDSEscape(value){return String(value??"").replace(/[&<>\"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"})[c])}
function stxDSId(prefix){return `${prefix}${Math.floor(random()*1e9)}`}
function stxDSNowCycle(){return Math.floor(state.simTime/Math.max(1,state.commandCycle||45))+1}
function stxDSBaseSpec(baseOrType){return STX_DS_BASE_TYPES[typeof baseOrType==="string"?baseOrType:baseOrType?.type]||STX_DS_BASE_TYPES.logistics}
function stxDSBases(owner=null,operationalOnly=false){return(state.deepSpaceBases||[]).filter(b=>(owner===null||b.owner===owner)&&(!operationalOnly||b.status==="operational")&&b.status!=="cancelled")}
function stxDSActiveBaseCount(owner){return stxDSBases(owner).filter(b=>b.status!=="wreck").length}
function stxDSBase(id){return(state.deepSpaceBases||[]).find(b=>b.id===id)||null}
function stxDSOperation(id){return(state.deepSpaceOperations||[]).find(o=>o.id===id&&o.active!==false)||null}
function stxDSDisplayedIndustry(owner){return Number((Number(empireIndustry(owner))||0).toFixed(1))}
function stxDSPointDistance(a,b){return Math.hypot((a?.x||0)-(b?.x||0),(a?.y||0)-(b?.y||0))}
function stxDSOwnerAtWar(a,b){return a!==b&&typeof empiresAtWar==="function"&&empiresAtWar(a,b)}
function stxDSNearestWorld(point,owner=null){return state.planets.filter(p=>owner===null||p.owner===owner).sort((a,b)=>stxDSPointDistance(a,point)-stxDSPointDistance(b,point))[0]||null}
function stxDSFleetType(f){if(f.fleetType)return f.fleetType;const role=String(f.role||"").toLowerCase(),strength=Number(f.strength)||0;f.fleetType=role.includes("patrol")?"Patrol Flotilla":strength>=48?"Heavy Battle Line":strength>=32?"Expeditionary Fleet":strength>=20?"Fast Response Fleet":"Escort Group";return f.fleetType}
function stxDSFlagship(f){if(f.flagship)return f.flagship;const names=["Imperious","Resolute","Astra Invicta","Vigilant","Crown of Dawn","Meridian","Defiant","Sovereign"];let h=0;for(const c of String(f.id||f.name))h=(h*33+c.charCodeAt(0))>>>0;f.flagship=names[h%names.length];return f.flagship}

function stxDSPersist(){
  const e=empire(0);if(!e)return;
  e.deepSpaceState={version:STX_DS_VERSION,bases:state.deepSpaceBases||[],battles:state.deepSpaceBattles||[],operations:state.deepSpaceOperations||[],incidents:state.deepSpaceIncidents||[],peaceOffers:state.deepSpacePeaceOffers||[],convoys:state.deepSpaceConvoys||[]};
}
function stxDSEnsureBase(b){
  const spec=stxDSBaseSpec(b);b.type=STX_DS_BASE_TYPES[b.type]?b.type:"logistics";b.tier=clamp(Number(b.tier)||1,1,3);b.status=b.status||"operational";b.maxHp=Math.max(1,Number(b.maxHp)||spec.maxHp+(b.tier-1)*44);b.hp=clamp(Number.isFinite(b.hp)?b.hp:b.maxHp,0,b.maxHp);b.supplyReadiness=clamp(Number.isFinite(b.supplyReadiness)?b.supplyReadiness:.65,0,1);b.dockedFleetIds=Array.isArray(b.dockedFleetIds)?b.dockedFleetIds:[];b.serviceQueue=Array.isArray(b.serviceQueue)?b.serviceQueue:[];b.activity=Array.isArray(b.activity)?b.activity:[];b.region=b.region||stxDSRegionName(b.x,b.y);b.name=b.name||stxDSBaseName(b.type,b.region);b.nextSupplyAt=Number.isFinite(b.nextSupplyAt)?b.nextSupplyAt:state.simTime+rand(6,16);b.lastSupplyAt=Number.isFinite(b.lastSupplyAt)?b.lastSupplyAt:state.simTime;b.tradeThroughput=Number(b.tradeThroughput)||0;b.blockedShipments=Number(b.blockedShipments)||0;
  if(b.project){b.project.need=b.project.need||{...spec.cost};b.project.delivered=b.project.delivered||Object.fromEntries(Object.keys(b.project.need).map(r=>[r,0]));b.project.progress=Number(b.project.progress)||0;b.project.phase=b.project.phase||"authorized";b.project.shipmentIds=Array.isArray(b.project.shipmentIds)?b.project.shipmentIds:[]}
  return b;
}
function stxDSEnsureState(reset=false){
  if(!state.empires?.length)return;
  const saved=!reset&&empire(0)?.deepSpaceState;
  if(reset||!saved||saved.version!==STX_DS_VERSION){state.deepSpaceBases=[];state.deepSpaceBattles=[];state.deepSpaceOperations=[];state.deepSpaceIncidents=[];state.deepSpacePeaceOffers=[];state.deepSpaceConvoys=[]}
  else{state.deepSpaceBases=saved.bases||[];state.deepSpaceBattles=saved.battles||[];state.deepSpaceOperations=saved.operations||[];state.deepSpaceIncidents=saved.incidents||[];state.deepSpacePeaceOffers=saved.peaceOffers||[];state.deepSpaceConvoys=saved.convoys||[]}
  state.deepSpaceBases.forEach(stxDSEnsureBase);state.deepSpaceBattles=Array.isArray(state.deepSpaceBattles)?state.deepSpaceBattles:[];state.deepSpaceOperations=Array.isArray(state.deepSpaceOperations)?state.deepSpaceOperations:[];state.deepSpaceIncidents=Array.isArray(state.deepSpaceIncidents)?state.deepSpaceIncidents:[];state.deepSpacePeaceOffers=Array.isArray(state.deepSpacePeaceOffers)?state.deepSpacePeaceOffers:[];state.deepSpaceConvoys=Array.isArray(state.deepSpaceConvoys)?state.deepSpaceConvoys:[];
  state.planets.forEach(p=>p.deepSpaceProjectIds=Array.isArray(p.deepSpaceProjectIds)?p.deepSpaceProjectIds:[]);
  state.fleets.forEach(f=>{stxDSFleetType(f);stxDSFlagship(f);f.deepSpaceBaseId=f.deepSpaceBaseId||null;f.deepSpaceOperationId=f.deepSpaceOperationId||null;f.preparedUntil=Number(f.preparedUntil)||0;f.preparedCoordination=Number(f.preparedCoordination)||0;f.maxServiceStrength=Math.max(Number(f.maxServiceStrength)||0,Number(f.strength)||0)});stxDSPersist();
}

function stxDSRegionName(x,y){
  const nearest=state.planets?.slice().sort((a,b)=>Math.hypot(a.x-x,a.y-y)-Math.hypot(b.x-x,b.y-y)).slice(0,2)||[];
  if(nearest.length>1&&Math.hypot(nearest[0].x-x,nearest[0].y-y)<900&&Math.hypot(nearest[1].x-x,nearest[1].y-y)<1150){const a=nearest[0].name.split(" ")[0],b=nearest[1].name.split(" ")[0];return `${a}–${b} Corridor`}
  if(nearest[0])return `${nearest[0].name.split(" ")[0]} ${x<(WORLD?.w||10000)*.32?"Reach":x>(WORLD?.w||10000)*.68?"Marches":"Passage"}`;
  return x<(WORLD?.w||10000)/2?"Western Reaches":"Eastern Marches";
}
function stxDSBaseName(type,region){const list=STX_DS_NAME_PREFIX[type]||STX_DS_NAME_PREFIX.logistics;let h=0;for(const c of `${region}:${type}`)h=(h*31+c.charCodeAt(0))>>>0;return `${list[h%list.length]} ${roman(1+h%3)}`}
function stxDSClearSite(x,y){
  const nearest=state.planets.slice().sort((a,b)=>Math.hypot(a.x-x,a.y-y)-Math.hypot(b.x-x,b.y-y))[0],base=stxDSBases().slice().sort((a,b)=>Math.hypot(a.x-x,a.y-y)-Math.hypot(b.x-x,b.y-y))[0];let nx=clamp(x,180,WORLD.w-180),ny=clamp(y,180,WORLD.h-180);
  if(nearest&&Math.hypot(nearest.x-nx,nearest.y-ny)<230){const a=Math.atan2(ny-nearest.y,nx-nearest.x)+.75;nx=nearest.x+Math.cos(a)*280;ny=nearest.y+Math.sin(a)*280}
  if(base&&Math.hypot(base.x-nx,base.y-ny)<190){const a=Math.atan2(ny-base.y,nx-base.x)+1.1;nx=base.x+Math.cos(a)*230;ny=base.y+Math.sin(a)*230}
  return{x:clamp(nx,180,WORLD.w-180),y:clamp(ny,180,WORLD.h-180)};
}
function stxDSGenerateSites(p,type){
  if(!p)return[];const friends=owned(p.owner).filter(q=>q!==p).sort((a,b)=>dist(p,a)-dist(p,b)),rivals=state.planets.filter(q=>q.owner!==null&&q.owner!==p.owner).sort((a,b)=>dist(p,a)-dist(p,b)),neutral=state.planets.filter(q=>q.owner===null).sort((a,b)=>dist(p,a)-dist(p,b));let raw=[];
  if(type==="military"){
    const targets=[rivals[0],rivals[1],neutral.find(q=>rivals[0]&&dist(q,rivals[0])<900)].filter(Boolean);raw=targets.map((q,i)=>({x:p.x+(q.x-p.x)*(.48+i*.07),y:p.y+(q.y-p.y)*(.48+i*.07),reason:i?`Covers a contested approach toward ${q.name}`:`Near the ${q.name} frontier`}));
  }else if(type==="trade"){
    const a=friends[0]||rivals[0],b=friends[1]||friends[0]||rivals[1];raw=[a,b,a&&b?{x:(a.x+b.x)/2,y:(a.y+b.y)/2,name:`${a.name}–${b.name}`} : null].filter(Boolean).map((q,i)=>q.x!==undefined&&q.name?{x:(p.x+q.x)/2,y:(p.y+q.y)/2,reason:`Intersects commerce between ${p.name} and ${q.name}`}:{x:(p.x+q.x)/2+(i-1)*90,y:(p.y+q.y)/2+(1-i)*70,reason:`Serves the ${p.name}–${q.name} trade route`});
  }else if(type==="logistics"){
    const far=friends.slice().sort((a,b)=>dist(p,b)-dist(p,a));raw=(far.length?far:[neutral[0],rivals[0]]).filter(Boolean).slice(0,3).map((q,i)=>({x:p.x+(q.x-p.x)*(.45+i*.08),y:p.y+(q.y-p.y)*(.45+i*.08),reason:`Shortens the long route from ${p.name} toward ${q.name}`}));
  }else{
    const gaps=[rivals[0],neutral[0],rivals[1]||neutral[1]].filter(Boolean);raw=gaps.map((q,i)=>({x:p.x+(q.x-p.x)*(.55+i*.06),y:p.y+(q.y-p.y)*(.55+i*.06),reason:`Observes the poorly charted approach toward ${q.name}`}));
  }
  while(raw.length<3){const a=(raw.length/3)*Math.PI*2+.7;raw.push({x:p.x+Math.cos(a)*(520+raw.length*90),y:p.y+Math.sin(a)*(520+raw.length*90),reason:"Covers an open strategic gap"})}
  return raw.slice(0,3).map((site,i)=>{const pos=stxDSClearSite(site.x,site.y),region=stxDSRegionName(pos.x,pos.y);return{...pos,region,name:stxDSBaseName(type,region),reason:site.reason,distance:Math.round(Math.hypot(pos.x-p.x,pos.y-p.y))}});
}
function stxDSCanSponsor(p,type){const industryReady=type!=="military"||stxDSDisplayedIndustry(p?.owner)>=STX_DS_MILITARY_INDUSTRY_UNLOCK;return!!p&&p.owner!==null&&!p.underAttack&&(p.infra?.factory||0)>=1&&(p.infra?.shipyard||0)>=1&&industryReady&&stxDSActiveBaseCount(p.owner)<STX_DS_MAX_BASES_PER_OWNER&&!stxDSBases(p.owner).some(b=>b.sponsorPlanetId===p.id&&b.type===type&&b.status==="construction")}
function stxDSProjectIncoming(base,r=null){return state.ships.filter(s=>s.stxDeepTransit&&s.deepBaseId===base.id&&s.stxDeepMission==="construction").reduce((n,s)=>n+(r?Number(s.cargo?.[r]||0):Object.values(s.cargo||{}).reduce((a,v)=>a+Number(v||0),0)),0)}
function stxDSProjectRatio(base){const q=base.project,total=Object.values(q?.need||{}).reduce((n,v)=>n+v,0)||1,got=Object.keys(q?.need||{}).reduce((n,r)=>n+Math.min(q.need[r],q.delivered?.[r]||0),0);return clamp(got/total,0,1)}
function stxDSProjectReady(base){const q=base.project;return!!q&&Object.keys(q.need||{}).every(r=>(q.delivered?.[r]||0)>=q.need[r]-.01)}
function stxDSAddActivity(base,text,tone=""){base.activity.unshift({text,time:state.simTime,tone});base.activity=base.activity.slice(0,14)}
function stxDSQueueBase(p,type,site,mandated=false){
  if(!stxDSCanSponsor(p,type)||!site)return false;const spec=stxDSBaseSpec(type),creditCost=Math.ceil(Object.values(spec.cost).reduce((n,v)=>n+v,0)*.13),e=empire(p.owner);if((e?.credits||0)<creditCost){if(p.owner===0)showToast(`Need ${creditCost} credits to authorize this base`);return false}e.credits-=creditCost;
  const b=stxDSEnsureBase({id:stxDSId("dsb"),type,tier:1,name:site.name||stxDSBaseName(type,site.region),region:site.region||stxDSRegionName(site.x,site.y),x:site.x,y:site.y,owner:p.owner,sponsorPlanetId:p.id,status:"construction",hp:1,maxHp:spec.maxHp,supplyReadiness:0,createdAt:state.simTime,dockedFleetIds:[],serviceQueue:[],activity:[],project:{id:stxDSId("dsp"),phase:"authorized",progress:0,need:{...spec.cost},delivered:Object.fromEntries(Object.keys(spec.cost).map(r=>[r,0])),startedAt:state.simTime,nextDispatchAt:state.simTime,shipmentIds:[],mandated,creditCost}});
  state.deepSpaceBases.push(b);p.deepSpaceProjectIds.push(b.id);stxDSAddActivity(b,`${p.name} authorized the project and opened material assembly.`,"good");Object.entries(b.project.need).forEach(([r,a])=>{if((p.stock?.[r]||0)<a*.72)addOrder(p,`deep-base-${b.id}`,r,a,mandated?90:8,`${b.name} construction modules`)});
  if(p.owner===0){logEvent(`${p.name} authorized ${b.name} in the ${b.region}. A construction convoy must physically reach the site.`,"good");galacticNews(`${p.name.toUpperCase()} AUTHORIZES ${spec.short}`,`${b.name} will be assembled in the ${b.region}. Materials are collecting at ${p.name} before construction vessels depart.`,"trade",p.id)}
  stxDSCreateFrontierIncident(b);stxDSPersist();return b;
}
function stxDSCancelBase(base,reason="project cancelled"){
  if(!base||!["construction","operational"].includes(base.status))return;const sponsor=state.planets.find(p=>p.id===base.sponsorPlanetId);base.status="wreck";base.hp=0;base.destroyedAt=state.simTime;base.wreckUntil=state.simTime+70;base.destroyedReason=reason;if(sponsor)sponsor.deepSpaceProjectIds=sponsor.deepSpaceProjectIds.filter(id=>id!==base.id);state.ships.filter(s=>s.stxDeepTransit&&s.deepBaseId===base.id).forEach(s=>s.stxCancelled=true);stxDSAddActivity(base,reason,"danger");
}
function stxDSCreateTransit({type="construction",owner,fromPoint,targetPoint,fromPlanetId=null,toPlanetId=null,baseId=null,fleetId=null,mission,cargo={},strength=0,vesselName:shipName=null,speedBoost=1,extra={}}){
  if(state.ships.length>=280)return null;const distance=Math.max(1,stxDSPointDistance(fromPoint,targetPoint)),baseSpeed={construction:68,freighter:88,supply:96,fleet:86,patrol:112}[type]||78,s={id:stxDSId("sds"),type,from:fromPlanetId||toPlanetId,to:toPlanetId||fromPlanetId,x:fromPoint.x,y:fromPoint.y,startX:fromPoint.x,startY:fromPoint.y,targetX:targetPoint.x,targetY:targetPoint.y,progress:0,distance,speed:baseSpeed*(1+(empire(owner)?.tech?.propulsion||0)*.08)*speedBoost,owner,cargo:{...cargo},strength,phase:rand(0,6.28),vesselName:shipName||vesselName(type),fleetId,deepBaseId:baseId,stxDeepTransit:true,stxDeepMission:mission,...extra};state.ships.push(s);return s;
}
function stxDSSourceProject(base,immediate=false){
  const q=base.project,p=state.planets.find(x=>x.id===base.sponsorPlanetId);if(!q||!p||base.status!=="construction"||(!immediate&&state.simTime<(q.nextDispatchAt||0)))return;const inbound=state.ships.filter(s=>s.stxDeepTransit&&s.deepBaseId===base.id&&s.stxDeepMission==="construction").length;if(inbound>=3)return;
  const needs=Object.keys(q.need).map(r=>({r,left:Math.max(0,q.need[r]-(q.delivered[r]||0)-stxDSProjectIncoming(base,r))})).filter(x=>x.left>.1).sort((a,b)=>b.left-a.left);
  for(const item of needs){if(state.ships.filter(s=>s.stxDeepTransit&&s.deepBaseId===base.id&&s.stxDeepMission==="construction").length>=3)break;const floor=["components","equipment"].includes(item.r)?6:12,available=Math.max(0,(p.stock?.[item.r]||0)-floor),amount=Math.min(item.left,available,36);if(amount<=.1){const open=p.orders?.some(o=>o.status==="waiting"&&o.type===`deep-base-${base.id}`&&o.resource===item.r);if(!open)addOrder(p,`deep-base-${base.id}`,item.r,item.left,q.mandated?90:8,`${base.name} construction modules`);continue}p.stock[item.r]-=amount;const first=q.shipmentIds.length===0,s=stxDSCreateTransit({type:first?"construction":"freighter",owner:base.owner,fromPoint:p,targetPoint:base,fromPlanetId:p.id,baseId:base.id,mission:"construction",cargo:{[item.r]:amount},vesselName:first?`${p.name} Construction Convoy`:vesselName("freighter"),speedBoost:first ? .86 : 1});if(!s){p.stock[item.r]+=amount;break}q.shipmentIds.push(s.id);q.phase=first?"convoy en route":"freight en route";stxDSAddActivity(base,`${s.vesselName} departed ${p.name} with ${Math.round(amount)} ${RESOURCE_LABEL[item.r]||item.r}.`,"good")}
  q.nextDispatchAt=state.simTime+(q.mandated?2.5:4.6);
}
function stxDSArriveConstruction(s,base){
  if(!base?.project||base.status!=="construction")return;Object.entries(s.cargo||{}).forEach(([r,v])=>base.project.delivered[r]=Math.min(base.project.need[r]||Infinity,(base.project.delivered[r]||0)+Number(v||0)));base.project.phase=stxDSProjectReady(base)?"final assembly":"site assembly";stxDSAddActivity(base,`${s.vesselName} reached the site and delivered ${Object.entries(s.cargo||{}).map(([r,v])=>`${Math.round(v)} ${RESOURCE_LABEL[r]||r}`).join(", ")}.`,"good");if(base.owner===0)stxActivity(`${s.vesselName} delivered construction cargo directly to ${base.name}.`,base.sponsorPlanetId,null,"good")
}
function stxDSCommission(base){
  const spec=stxDSBaseSpec(base),p=state.planets.find(x=>x.id===base.sponsorPlanetId);base.status="operational";base.hp=base.maxHp;delete base.project;base.supplyReadiness=.72;base.commissionedAt=state.simTime;base.nextSupplyAt=state.simTime+5;if(p)p.deepSpaceProjectIds=p.deepSpaceProjectIds.filter(id=>id!==base.id);stxDSAddActivity(base,"Commissioning completed; the first operational watch assumed control.","good");if(base.owner===0){logEvent(`${base.name} is operational in the ${base.region}. It is now a normal fleet destination.`,"good");galacticNews(`${base.name.toUpperCase()} COMMISSIONED`,`${spec.short} operations have begun in the ${base.region}. Supply ships are establishing a permanent service route.`,"good",p?.id||null)}
}
function stxDSTickProjects(dt){
  stxDSBases().filter(b=>b.status==="construction").forEach(base=>{const q=base.project,p=state.planets.find(x=>x.id===base.sponsorPlanetId);if(!q||!p)return;stxDSSourceProject(base,false);const ratio=stxDSProjectRatio(base);if(ratio>.02&&q.phase==="authorized")q.phase="site deliveries";if(ratio>=.22){q.phase=stxDSProjectReady(base)?"final assembly":"site assembly";const capacity=1+(p.infra?.factory||0)*.18+(p.infra?.shipyard||0)*.22;q.progress=Math.max(q.progress,ratio*.36);q.progress=Math.min(stxDSProjectReady(base) ? .94 : .88,q.progress+dt*.0036*capacity*Math.max(.3,ratio))}if(stxDSProjectReady(base)&&q.progress>=.92){q.phase="commissioning";q.progress=.97;q.commissioningEnds=q.commissioningEnds||state.simTime+Math.max(4,8-(p.infra?.shipyard||0)*.55);if(state.simTime>=q.commissioningEnds)stxDSCommission(base)}});
}

function stxDSSupplySource(base){return owned(base.owner).filter(p=>!p.underAttack).sort((a,b)=>stxDSPointDistance(a,base)-stxDSPointDistance(b,base))[0]||null}
function stxDSSendSupply(base){
  const p=stxDSSupplySource(base);if(!p)return false;const wanted={helium:8,components:5,equipment:3},cargo={};for(const [r,a] of Object.entries(wanted)){const take=Math.min(a,Math.max(0,(p.stock?.[r]||0)-(["components","equipment"].includes(r)?5:10)));if(take>0){p.stock[r]-=take;cargo[r]=take}}
  if(!Object.keys(cargo).length)return false;const s=stxDSCreateTransit({type:"supply",owner:base.owner,fromPoint:p,targetPoint:base,fromPlanetId:p.id,baseId:base.id,mission:"supply",cargo,vesselName:`${p.name} Base Tender`,speedBoost:1.04});if(!s){Object.entries(cargo).forEach(([r,v])=>p.stock[r]+=v);return false}base.nextSupplyAt=state.simTime+rand(18,28);stxDSAddActivity(base,`${s.vesselName} launched from ${p.name}.`);return true;
}
function stxDSArriveSupply(s,base){const amount=Object.values(s.cargo||{}).reduce((n,v)=>n+Number(v||0),0),gain=clamp(.12+amount*.006,.12,.28);base.supplyReadiness=clamp(base.supplyReadiness+gain,0,1);base.lastSupplyAt=state.simTime;base.tradeThroughput+=amount;base.blockedShipments=Math.max(0,base.blockedShipments-1);stxDSAddActivity(base,`${s.vesselName} restored ${Math.round(gain*100)}% supply readiness.`,"good")}
function stxDSLaunchBonus(base){if(!base||base.status!=="operational")return{speed:0,coordination:0};const supply=base.supplyReadiness,tier=base.tier||1;if(base.type!=="military")return{speed:(.04+tier*.02)*supply,coordination:(.025+tier*.012)*supply};return{speed:(.10+tier*.035)*supply,coordination:(.08+tier*.025)*supply}}
function stxDSUndockFleet(f){if(!f)return;const base=stxDSBase(f.deepSpaceBaseId);if(base)base.dockedFleetIds=base.dockedFleetIds.filter(id=>id!==f.id);f.deepSpaceBaseId=null;f.deepSpaceOperationId=null}
function stxDSDockFleet(f,base){
  if(!f||!base||base.status!=="operational")return false;stxDSUndockFleet(f);f.location=base.sponsorPlanetId;f.deepSpaceBaseId=base.id;f.readiness="servicing";f.serviceUntil=state.simTime+Math.max(4,stxDSBaseSpec(base).service-(base.tier||1)*.7);f.status=`Servicing at ${base.name}`;base.dockedFleetIds=[...new Set([...base.dockedFleetIds,f.id])];base.serviceQueue.push({fleetId:f.id,until:f.serviceUntil});stxDSAddActivity(base,`${f.name} docked for repair, fuel, and campaign preparation.`,"good");return true;
}
function stxDSTickBases(dt){
  stxDSBases().forEach(base=>{if(base.status!=="operational")return;const spec=stxDSBaseSpec(base),blockade=state.deepSpaceOperations.some(o=>o.active!==false&&o.owner!==base.owner&&o.targetType==="base"&&o.targetId===base.id&&stxDSOwnerAtWar(o.owner,base.owner));base.supplyReadiness=clamp(base.supplyReadiness-dt*spec.supplyDecay*(blockade?4.2:1),0,1);base.tradeThroughput=Math.max(0,base.tradeThroughput-dt*.045);base.serviceQueue=base.serviceQueue.filter(x=>(x.until||0)>state.simTime);base.dockedFleetIds=base.dockedFleetIds.filter(id=>{const f=fleetRecord(id);return f&&!f.destroyed&&f.deepSpaceBaseId===base.id});if(base.supplyReadiness<.92&&state.simTime>=(base.nextSupplyAt||0))stxDSSendSupply(base);
    base.dockedFleetIds.forEach(id=>{const f=fleetRecord(id);if(!f)return;const health=clamp(base.supplyReadiness,.08,1);if(f.strength<(f.maxServiceStrength||f.strength))f.strength=Math.min(f.maxServiceStrength,f.strength+dt*.045*(base.tier||1)*health);if(f.readiness==="servicing"&&state.simTime>=f.serviceUntil){const bonus=stxDSLaunchBonus(base);f.readiness="base prepared";f.supplyReadiness=1;f.preparedUntil=state.simTime+Math.max(1,state.commandCycle||45)*2;f.preparedCoordination=bonus.coordination;f.preparedSpeed=bonus.speed;f.status=`BASE PREPARED at ${base.name} · 2 cycles`;stxDSAddActivity(base,`${f.name} is BASE PREPARED for two strategic cycles.`,"good");if(f.owner===0)showToast(`${f.name} · BASE PREPARED for 2 cycles`)}});
    if(base.type==="trade"&&base.supplyReadiness>.15){const revenue=dt*.0015*(base.tier||1)*base.supplyReadiness;empire(base.owner).credits+=revenue;base.tradeThroughput+=dt*.004*(base.tier||1)}
  });
  state.fleets.forEach(f=>{if(f.preparedUntil&&state.simTime>=f.preparedUntil){f.preparedUntil=0;f.preparedCoordination=0;f.preparedSpeed=0;if(f.readiness==="base prepared"){f.readiness="patrol";f.status=f.deepSpaceBaseId?`Stationed at ${stxDSBase(f.deepSpaceBaseId)?.name||"deep-space base"}`:"Prepared-base advantage expired"}}});
}

function stxDSCurrentFleetPoint(f){
  const ship=state.ships.find(s=>s.fleetId===f?.id);if(ship)return{x:ship.x,y:ship.y,ship};const op=stxDSOperation(f?.deepSpaceOperationId);if(op)return{x:op.x,y:op.y,operation:op};const base=stxDSBase(f?.deepSpaceBaseId);if(base)return{x:base.x,y:base.y,base};return state.planets.find(p=>p.id===f?.location)||state.planets.find(p=>p.id===f?.homePort)||null;
}
function stxDSDestinationPoint(destination){if(destination?.base)return destination.base;if(destination?.type==="deepBase")return stxDSBase(destination.id);return destination?.planet||destination||null}
function stxDSDispatchFleet(f,destination,kind,meta={}){
  if(!f||f.destroyed)return false;const target=stxDSDestinationPoint(destination),from=stxDSCurrentFleetPoint(f);if(!target||!from)return false;const transit=state.ships.find(s=>s.fleetId===f.id),oldOperation=stxDSOperation(f.deepSpaceOperationId);if(typeof stxFCADetachBattle==="function")stxFCADetachBattle(f);if(f.stxRefit)f.stxRefit=null;stxDSUndockFleet(f);if(oldOperation)oldOperation.active=false;
  const destinationBase=destination?.base||(destination?.type==="deepBase"?stxDSBase(destination.id):null),finalPlanet=destinationBase?null:destination?.planet||destination,mission=meta.stxDeepMission||(destinationBase?(kind==="invade"||kind==="assault"?"assault":kind==="raid"?"raid":"dock"):kind==="raid"?"raid":kind==="blockade"?"blockade":"planet"),prepared=f.preparedUntil>state.simTime?Number(f.preparedSpeed)||0:0,speedBoost=(meta.speedBoost||1)*(1+prepared);
  if(transit)state.ships.splice(state.ships.indexOf(transit),1);f.location=null;f.readiness="inbound";f.status=mission==="assault"?`Assaulting ${destinationBase.name}`:mission==="dock"?`Inbound to ${destinationBase.name}`:mission==="raid"?`Raiding commerce near ${destination.name||target.name}`:mission==="blockade"?`Establishing blockade at ${destination.name||target.name}`:`${kind==="invade"?"Invading":"Redeploying to"} ${destination.name||target.name}`;
  const sourcePlanet=stxDSNearestWorld(from,f.owner)||state.planets.find(p=>p.id===f.homePort),s=stxDSCreateTransit({type:"fleet",owner:f.owner,fromPoint:from,targetPoint:target,fromPlanetId:sourcePlanet?.id||f.homePort,toPlanetId:finalPlanet?.id||destinationBase?.sponsorPlanetId,baseId:destinationBase?.id||null,fleetId:f.id,mission,strength:f.strength,vesselName:f.name,speedBoost,extra:{...meta,stxFinalPlanetId:finalPlanet?.id||null,stxDestinationType:destinationBase?"base":"planet",stxFleetOrderKind:kind,servicePurpose:kind==="invade"?"planetary invasion":kind==="raid"||kind==="blockade"?"economic warfare":"fleet concentration",stationTargetId:destination?.facility?.id||meta.stationTargetId,status:f.status}});return!!s;
}

function stxDSStartBattle(s,base){
  if(!base||base.status!=="operational"||base.owner===s.owner)return false;let b=state.deepSpaceBattles.find(x=>x.baseId===base.id);const f=fleetRecord(s.fleetId);if(b){if(s.owner===b.attacker){b.attackerStrength+=s.strength;b.attackerInitial+=s.strength;b.attackerFleetIds.push(s.fleetId)}else if(s.owner===b.defender){b.defenderStrength+=s.strength;b.defenderInitial+=s.strength;b.defenderFleetIds.push(s.fleetId)}if(f)f.status=`Engaged at ${base.name}`;return true}
  const defenders=base.dockedFleetIds.map(fleetRecord).filter(x=>x&&!x.destroyed),defFleet=defenders.reduce((n,x)=>n+(x.strength||0),0),readiness=clamp(base.supplyReadiness,.08,1),fort=(10+(base.tier||1)*15+(base.type==="military"?22:0))*readiness;b={id:stxDSId("dsbtl"),baseId:base.id,x:base.x,y:base.y,attacker:s.owner,defender:base.owner,attackerStrength:Math.max(1,s.strength)*(1+(f?.preparedUntil>state.simTime?(f.preparedCoordination||0):0)),defenderStrength:Math.max(5,defFleet+fort),attackerInitial:Math.max(1,s.strength),defenderInitial:Math.max(5,defFleet+fort),attackerFleetIds:[s.fleetId].filter(Boolean),defenderFleetIds:defenders.map(x=>x.id),elapsed:0,maxDuration:20+rand(5,10),fxClock:0};state.deepSpaceBattles.push(b);base.underAttack=true;[f,...defenders].filter(Boolean).forEach(x=>x.status=`Engaged at ${base.name}`);galacticNews(`BATTLE OF THE ${base.region.toUpperCase()}`,`${empire(s.owner).name} forces have begun an assault on ${base.name}. The independent facility will be destroyed, not captured, if its defenses fail.`,"danger",base.sponsorPlanetId);return true;
}
function stxDSReturnFleet(f,point,owner,status="Returning from deep-space operations"){
  if(!f||f.destroyed)return;const home=owned(owner).sort((a,b)=>stxDSPointDistance(a,point)-stxDSPointDistance(b,point))[0];if(!home){f.destroyed=true;return}f.status=status;stxDSCreateTransit({type:"fleet",owner,fromPoint:point,targetPoint:home,fromPlanetId:home.id,toPlanetId:home.id,fleetId:f.id,mission:"planet",strength:f.strength,vesselName:f.name,speedBoost:1.12,extra:{stxFinalPlanetId:home.id,retreat:true,status}})
}
function stxDSReactToDestruction(base,attacker){
  state.empires.forEach(e=>{if(e.id===attacker||e.id===base.owner)return;const commercial=e.foreignPolicy?.commercialism||.4,delta=base.type==="trade"?-Math.round(3+commercial*5):-Math.round(1+(e.foreignPolicy?.caution||.4)*3);adjustRelation(e.id,attacker,delta/100)});if(typeof stxRDAddGrievance==="function")stxRDAddGrievance(base.owner,attacker,"destroyed deep-space base",`${empire(attacker).name} destroyed ${base.name} in the ${base.region}`,5,base.id);
}
function stxDSMarkWarGoal(w,type,targetId){if(!w?.warGoal||w.warGoal.status!=="active")return;const goal=w.warGoal;if(goal.type===type&&(goal.targetId===targetId||!goal.targetId)){goal.status="achieved";goal.achievedAt=state.simTime;stxDSCreatePeaceOffer(w)}}
function stxDSDestroyBase(base,attacker,reason="destroyed in battle"){
  if(!base||base.status==="wreck")return;base.status="wreck";base.hp=0;base.destroyedAt=state.simTime;base.wreckUntil=state.simTime+180;base.destroyedBy=attacker;base.destroyedReason=reason;base.underAttack=false;base.dockedFleetIds=[];state.deepSpaceOperations.filter(o=>o.targetType==="base"&&o.targetId===base.id).forEach(o=>o.active=false);stxDSAddActivity(base,reason,"danger");galacticNews(`FALL OF ${base.name.toUpperCase()}`,`${empire(attacker).name} destroyed the independent facility in the ${base.region}. Wreckage now marks the former strategic position, and regional routes are rerouting.`,"danger",base.sponsorPlanetId);stxDSReactToDestruction(base,attacker);const w=getWar(attacker,base.owner);if(w)stxDSMarkWarGoal(w,"destroy-base",base.id)
}
function stxDSResolveBattle(b){
  const base=stxDSBase(b.baseId);if(!base)return;const attackerWon=b.attackerStrength>b.defenderStrength*1.03,af=b.attackerFleetIds.map(fleetRecord).filter(Boolean),df=b.defenderFleetIds.map(fleetRecord).filter(Boolean),aEach=Math.max(.2,b.attackerStrength/Math.max(1,af.length)),dEach=Math.max(.2,b.defenderStrength/Math.max(1,df.length));af.forEach(f=>{f.strength=aEach;if(f.strength<.65){f.destroyed=true;f.status=`Lost at ${base.name}`}else stxDSReturnFleet(f,base,b.attacker,attackerWon?`Returning after destroying ${base.name}`:`Retreating from ${base.name}`)});df.forEach(f=>{f.strength=dEach;if(f.strength<.65){f.destroyed=true;f.status=`Lost defending ${base.name}`}else if(attackerWon)stxDSReturnFleet(f,base,b.defender,`Evacuating the wreck of ${base.name}`);else stxDSDockFleet(f,base)});if(attackerWon)stxDSDestroyBase(base,b.attacker,"destroyed after a fleet-scale assault");else{base.hp=Math.max(1,base.hp-base.maxHp*clamp(1-b.defenderStrength/b.defenderInitial,.08,.55));base.underAttack=false;galacticNews(`${base.name.toUpperCase()} HOLDS`,`${empire(base.owner).name} retained the independent facility after a major battle in the ${base.region}.`,"good",base.sponsorPlanetId)}
}
function stxDSTickBattles(dt){for(let i=state.deepSpaceBattles.length-1;i>=0;i--){const b=state.deepSpaceBattles[i],base=stxDSBase(b.baseId);if(!base){state.deepSpaceBattles.splice(i,1);continue}b.elapsed+=dt;b.fxClock+=dt;const ae=empire(b.attacker),de=empire(b.defender),supply=clamp(base.supplyReadiness,.08,1),ad=(.16+b.attackerStrength*.006)*(1+(ae?.tech?.weapons||0)*.1),dd=(.18+b.defenderStrength*.0057)*(1+(de?.tech?.weapons||0)*.1)*(1+(base.tier||1)*.08)*supply;b.defenderStrength=Math.max(0,b.defenderStrength-ad*dt*rand(.83,1.15));b.attackerStrength=Math.max(0,b.attackerStrength-dd*dt*rand(.83,1.15));if(b.fxClock>.25){b.fxClock=0;state.effects.push({type:random()<.3?"shock":"spark",x:base.x+rand(-90,90),y:base.y+rand(-65,65),life:.55,maxLife:.55,size:rand(6,15),color:random()<.5?empire(b.attacker).color:empire(b.defender).color})}if(b.attackerStrength<=.6||b.defenderStrength<=.6||b.elapsed>=b.maxDuration){stxDSResolveBattle(b);state.deepSpaceBattles.splice(i,1)}}}

function stxDSEconomicTargets(kind,owner=0){
  const foes=new Set(state.wars.filter(w=>w.active&&(w.a===owner||w.b===owner)).map(w=>w.a===owner?w.b:w.a));if(kind==="blockade")return state.planets.filter(p=>p.owner!==null&&foes.has(p.owner)).sort((a,b)=>((b.tradeVolume||0)*2+(b.infra?.shipyard||0)*35+(b.infra?.factory||0)*18)-((a.tradeVolume||0)*2+(a.infra?.shipyard||0)*35+(a.infra?.factory||0)*18)).map(p=>({key:`planet:${p.id}`,type:"planet",id:p.id,name:p.name,planet:p,subtitle:`BLOCKADE · ${Math.round(p.tradeVolume||0)} trade volume · ${p.infra?.shipyard||0} shipyard tier`}));
  const bases=stxDSBases().filter(b=>foes.has(b.owner)&&b.status==="operational"&&["trade","logistics"].includes(b.type)).map(b=>({key:`deep-base:${b.id}`,type:"deepBase",id:b.id,name:b.name,base:b,planet:state.planets.find(p=>p.id===b.sponsorPlanetId),subtitle:`RAID COMMERCE · ${b.region} · ${Math.round(b.supplyReadiness*100)}% supplied`}));const worlds=state.planets.filter(p=>p.owner!==null&&foes.has(p.owner)&&((p.tradeVolume||0)>5||p.tradeStation||(p.infra?.shipyard||0)>0)).map(p=>({key:`planet:${p.id}`,type:"planet",id:p.id,name:`${p.name} Commercial Corridor`,planet:p,subtitle:`RAID COMMERCE · ${Math.round(p.tradeVolume||0)} route volume${p.tradeStation?" · trade station":""}`}));return[...bases,...worlds].sort((a,b)=>String(a.name).localeCompare(String(b.name)));
}
function stxDSStartOperation(f,destination,kind){
  const target=stxDSDestinationPoint(destination);if(!f||!target)return false;if(target.owner===f.owner||!stxDSOwnerAtWar(f.owner,target.owner)){stxDSReturnFleet(f,target,f.owner,"Returning after ceasefire");return false}const existing=state.deepSpaceOperations.find(o=>o.active!==false&&o.fleetId===f.id);if(existing)existing.active=false;const op={id:stxDSId("dso"),kind,owner:f.owner,fleetId:f.id,targetType:(destination.base||destination.type==="deepBase")?"base":"planet",targetId:destination.base?.id||destination.id,x:target.x,y:target.y,strength:f.strength,startedAt:state.simTime,endsAt:state.simTime+Math.max(1,state.commandCycle||45)*2,radius:kind==="blockade"?520:390,intercepted:0,rerouted:0,cargoDestroyed:0,active:true};state.deepSpaceOperations.push(op);f.location=destination.planet?.id||destination.id||stxDSNearestWorld(target,f.owner)?.id;f.deepSpaceOperationId=op.id;f.deepSpaceBaseId=null;f.readiness="combat patrol";f.status=kind==="blockade"?`Blockading ${destination.name}`:`Raiding commerce near ${destination.name}`;if(op.targetType==="base"){const b=stxDSBase(op.targetId);if(b)stxDSAddActivity(b,`${f.name} began hostile ${kind==="blockade"?"isolation":"commerce raiding"} operations.`,"danger")}return true;
}
function stxDSCargoValue(s){return Object.entries(s.cargo||{}).reduce((n,[r,v])=>n+Number(v||0)*(["components","equipment"].includes(r)?2.4:r==="helium"?1.7:1),0)}
function stxDSInterceptShip(s,op){
  if(s.stxIntercepted)return;s.stxIntercepted=true;const value=stxDSCargoValue(s),victim=empire(s.owner),cargo={...s.cargo};if(victim)victim.credits=Math.max(0,victim.credits-Math.min(18,value*.045));op.intercepted++;op.cargoDestroyed+=value;op.lostCargo=op.lostCargo||{};Object.entries(cargo).forEach(([resource,amount])=>op.lostCargo[resource]=(op.lostCargo[resource]||0)+Number(amount||0));const destination=state.planets.find(p=>p.id===s.to),order=s.orderId&&destination?.orders?.find(o=>o.id===s.orderId);if(order){order.status="waiting";order.stxLastLossAt=state.simTime;order.stxLastAttempt=state.simTime}const target=op.targetType==="base"?stxDSBase(op.targetId):state.planets.find(p=>p.id===op.targetId);if(target){target.blockedShipments=(target.blockedShipments||0)+1;target.raidLossLedger=Array.isArray(target.raidLossLedger)?target.raidLossLedger:[];target.raidLossLedger.unshift({time:state.simTime,attacker:op.owner,ship:s.vesselName||s.type,cargo});target.raidLossLedger=target.raidLossLedger.slice(0,18);if(target.tradeVolume!=null)target.tradeVolume=Math.max(0,target.tradeVolume-Math.min(4,1+value*.012));if(target.tradeThroughput!=null)target.tradeThroughput=Math.max(0,target.tradeThroughput-Math.min(10,2+value*.025))}if(op.owner===0&&op.intercepted<=3)logEvent(`${fleetRecord(op.fleetId)?.name||"Commerce raiders"} intercepted ${s.vesselName||"a cargo vessel"}; ${Math.round(value)} cargo value will not reach its destination.`,"warning");if(op.intercepted===3&&target)galacticNews(`${String(target.name||target.region).toUpperCase()} SHIPPING DISRUPTED`,`${fleetRecord(op.fleetId)?.name||empire(op.owner).name+" raiders"} have destroyed three cargoes. Orders are reopening, merchants are rerouting, and ${Object.entries(op.lostCargo).sort((a,b)=>b[1]-a[1])[0]?.[0]||"critical material"} shortages are now visible in local projects.`,"warning",target.id||target.sponsorPlanetId||null);if(typeof stxRDAddGrievance==="function"&&op.intercepted===1)stxRDAddGrievance(s.owner,op.owner,"commerce raiding",`${empire(op.owner).name} began intercepting wartime shipping`,3,op.targetId)
}
function stxDSCheckInterceptions(dt){
  const ops=state.deepSpaceOperations.filter(o=>o.active!==false);if(!ops.length)return;state.ships.forEach(s=>{if(s.stxIntercepted||s.owner==null||STX_MILITARY_TYPES.has(s.type))return;const civilian=s.commercial||STX_DS_FREIGHT_TYPES.has(s.type)||s.projectDelivery||s.stxDeepMission==="supply"||s.stxDeepMission==="construction";if(!civilian)return;for(const op of ops){if(op.owner===s.owner||!stxDSOwnerAtWar(op.owner,s.owner))continue;const targetPlanet=op.targetType==="planet"?state.planets.find(p=>p.id===op.targetId):null,inRoute=stxDSPointDistance(s,op)<op.radius||(op.kind==="blockade"&&targetPlanet&&(s.to===targetPlanet.id||s.from===targetPlanet.id)&&stxDSPointDistance(s,targetPlanet)<op.radius*1.35);if(!inRoute)continue;s.stxRaidExposure=(s.stxRaidExposure||0)+dt*(.55+op.strength/55)*(op.kind==="blockade"?1.3:1);if(!s.stxRerouted&&s.stxRaidExposure>.35&&random()<.18){s.stxRerouted=true;s.speed*=.78;s.phase+=1.4;op.rerouted++}if(s.stxRaidExposure>1.2&&random()<clamp(.15+op.strength/190,.18,.68))stxDSInterceptShip(s,op);break}})
}
function stxDSContestOperation(op,f,target){
  if(state.simTime<(op.nextControlCheckAt||0))return false;op.nextControlCheckAt=state.simTime+5;const defenders=(op.targetType==="base"?(target.dockedFleetIds||[]).map(fleetRecord):state.fleets.filter(x=>x.owner===target.owner&&x.location===target.id&&!x.destroyed&&!state.ships.some(s=>s.fleetId===x.id))).filter(x=>x&&!x.destroyed&&x.id!==f.id),fixed=op.targetType==="planet"?((target.orbitals?.base||0)*10+(target.orbitals?.station||0)*4):0,defense=defenders.reduce((n,x)=>n+(Number(x.strength)||0),fixed);if(defense<=0)return false;const attack=Math.max(.1,Number(f.strength)||0),aLoss=Math.min(attack*.22,Math.max(.35,defense*.045)),dLoss=Math.min(defense*.16,Math.max(.25,attack*.028));f.strength=Math.max(.35,attack-aLoss);defenders.forEach(d=>{d.maxServiceStrength=Math.max(d.maxServiceStrength||0,d.strength||0);d.strength=Math.max(.35,(d.strength||0)-dLoss/Math.max(1,defenders.length));d.status=`Contesting ${op.kind} near ${target.name}`});if(defense<attack*.72)return false;op.active=false;f.deepSpaceOperationId=null;f.battles=(f.battles||0)+1;defenders.forEach(d=>{d.battles=(d.battles||0)+1;d.victories=(d.victories||0)+1;d.status=`Holding against ${op.kind} at ${target.name}`});stxDSReturnFleet(f,op,op.owner,`Driven off from ${target.name}`);galacticNews(`${String(target.name).toUpperCase()} ${op.kind==="blockade"?"BLOCKADE BROKEN":"RAIDERS DRIVEN OFF"}`,`${empire(target.owner).name} defenders restored local space control and forced ${fleetRecord(op.fleetId)?.name||empire(op.owner).name+" forces"} to withdraw.`,"good",target.id||target.sponsorPlanetId||null);return true;
}
function stxDSTickOperations(){
  state.deepSpaceOperations.forEach(op=>{if(op.active===false)return;const f=fleetRecord(op.fleetId);if(!f||f.destroyed){op.active=false;return}op.strength=f.strength;const target=op.targetType==="base"?stxDSBase(op.targetId):state.planets.find(p=>p.id===op.targetId);if(!target||target.status==="wreck"){op.active=false;f.deepSpaceOperationId=null;stxDSReturnFleet(f,op,op.owner,"Returning from completed strategic operation");return}const war=getWar(op.owner,target.owner);if(!war){op.active=false;f.deepSpaceOperationId=null;stxDSReturnFleet(f,op,op.owner,"Returning under ceasefire");return}if(stxDSContestOperation(op,f,target))return;const heldFor=state.simTime-(op.startedAt??state.simTime);if(op.kind==="raid"&&op.targetType==="base"&&(op.intercepted>=3||target.supplyReadiness<=.3))stxDSMarkWarGoal(war,"break-trade-network",target.id);if(op.kind==="blockade"&&(op.intercepted>=3||heldFor>=Math.max(1,state.commandCycle||45)*.75))stxDSMarkWarGoal(war,"blockade",target.id);if(state.simTime>=op.endsAt){op.active=false;f.deepSpaceOperationId=null;stxDSReturnFleet(f,op,op.owner,`Returning after ${op.kind==="blockade"?"blockade":"commerce raid"}`)}});state.deepSpaceOperations=state.deepSpaceOperations.filter(o=>o.active!==false||state.simTime-(o.endsAt||0)<35)
}

function stxDSHandleTransitArrival(s){
  if(s.stxCancelled||s.stxIntercepted)return;const base=s.deepBaseId&&stxDSBase(s.deepBaseId);if(s.stxDeepMission==="construction")return stxDSArriveConstruction(s,base);if(s.stxDeepMission==="supply")return base&&stxDSArriveSupply(s,base);const f=fleetRecord(s.fleetId);if(!f)return;if(s.stxDeepMission==="dock")return stxDSDockFleet(f,base);if(s.stxDeepMission==="assault")return stxDSStartBattle(s,base);if(s.stxDeepMission==="raid"||s.stxDeepMission==="blockade"){const destination=base?{id:base.id,name:base.name,type:"deepBase",base,planet:state.planets.find(p=>p.id===base.sponsorPlanetId)}:{id:s.stxFinalPlanetId,name:state.planets.find(p=>p.id===s.stxFinalPlanetId)?.name||"target",planet:state.planets.find(p=>p.id===s.stxFinalPlanetId)};return stxDSStartOperation(f,destination,s.stxDeepMission)}const p=state.planets.find(x=>x.id===s.stxFinalPlanetId);if(p){s.stxDeepTransit=false;delete s.targetX;delete s.targetY;arriveShip(s,p)}
}
const STX_DS_tickShips=tickShips;
tickShips=function(dt){
  stxDSCheckInterceptions(dt);const special=state.ships.filter(s=>s.stxDeepTransit),regular=state.ships.filter(s=>!s.stxDeepTransit&&!s.stxIntercepted);state.ships=regular;STX_DS_tickShips(dt);const after=state.ships;state.ships=[...after,...special.filter(s=>!s.stxIntercepted&&!s.stxCancelled)];for(let i=state.ships.length-1;i>=0;i--){const s=state.ships[i];if(!s.stxDeepTransit)continue;s.progress+=s.speed*dt/Math.max(1,s.distance);const t=clamp(s.progress,0,1),ease=t*t*(3-2*t);s.x=s.startX+(s.targetX-s.startX)*ease+Math.sin(t*Math.PI)*Math.sin(s.phase)*22;s.y=s.startY+(s.targetY-s.startY)*ease-Math.sin(t*Math.PI)*Math.cos(s.phase)*22;if(s.progress>=1){state.ships.splice(i,1);stxDSHandleTransitArrival(s)}}
};

function stxDSBuildWarGoal(a,b,w){
  const policy=empire(a)?.foreignPolicy||{},pair=typeof stxRDPair==="function"?stxRDPair(a,b):null,recent=(pair?.grievances||[]).find(g=>g.holder===a&&g.against===b&&state.simTime-g.time<220&&/base|station|commerce|embargo/i.test(`${g.type} ${g.text}`)),enemyBases=stxDSBases(b,true),tradeBase=enemyBases.filter(x=>["trade","logistics"].includes(x.type)).sort((x,y)=>(y.tradeThroughput||0)-(x.tradeThroughput||0))[0],militaryBase=enemyBases.filter(x=>x.type==="military").sort((x,y)=>stxDSPointDistance(x,stxDSNearestWorld(x,a))-stxDSPointDistance(y,stxDSNearestWorld(y,a)))[0];let type="capture-planet",target=null,label="Punitive planetary campaign";
  if(recent&&enemyBases.length){target=enemyBases.find(x=>x.id===recent.targetId)||militaryBase||tradeBase||enemyBases[0];type="destroy-base";label=`Destroy ${target.name}`}
  else if((policy.commercialism||0)>.62&&(tradeBase||owned(b).some(p=>(p.tradeVolume||0)>8))){target=tradeBase||owned(b).sort((x,y)=>(y.tradeVolume||0)-(x.tradeVolume||0))[0];type=tradeBase?"break-trade-network":"blockade";label=tradeBase?`Break the ${target.region} trade network`:`Blockade ${target.name}`}
  else if((policy.aggression||0)>.62&&militaryBase){target=militaryBase;type="destroy-base";label=`Destroy ${target.name}`}
  else{target=owned(b).sort((x,y)=>((y.infra?.shipyard||0)*28+y.pop*24)-((x.infra?.shipyard||0)*28+x.pop*24))[0]||null;label=target?`Capture ${target.name}`:"Punitive expedition"}
  return{type,targetId:target?.id||null,label,region:target?.region||target?.name||null,status:"active",createdAt:state.simTime,limited:type!=="capture-planet"};
}
const STX_DS_declareWar=declareWar;
declareWar=function(a,b,reason="border dispute"){
  const pact=stxDSAgreementBetween("non-aggression",a,b);if(pact){pact.active=false;if(typeof stxRDAddGrievance==="function")stxRDAddGrievance(b,a,"broken non-aggression pact",`${empire(a).name} broke its non-aggression pact`,5)}const w=STX_DS_declareWar(a,b,reason);if(!w)return w;w.warGoal=w.warGoal||stxDSBuildWarGoal(a,b,w);w.stxLimitedWar=!!w.warGoal?.limited;galacticNews(`${empire(a).name.toUpperCase()} SETS WAR OBJECTIVE`,`${w.warGoal.label}. ${w.stxLimitedWar?"The conflict is presently limited to that strategic goal.":"Planetary conquest remains possible."}`,"warning",null);if(!stxDSAllianceGuard){stxDSAllianceGuard=true;try{state.empires.forEach(e=>{if(e.id===a||e.id===b||getWar(a,e.id))return;if(stxDSAgreementBetween("mutual-defense",e.id,b)&&owned(e.id).length)STX_DS_declareWar(e.id,a,`mutual defense of ${empire(b).name}`)})}finally{stxDSAllianceGuard=false}}return w;
};
function stxDSCreatePeaceOffer(w){if(!w||state.deepSpacePeaceOffers.some(o=>o.warId===w.id&&o.status==="pending"))return;const aggressor=Number.isFinite(w.aggressor)?w.aggressor:w.a,defender=aggressor===w.a?w.b:w.a,offer={id:stxDSId("dspo"),warId:w.id,from:aggressor,to:defender,status:"pending",createdAt:state.simTime,expiresAt:state.simTime+45,goal:w.warGoal?.label||"limited objective",autoResolveAt:state.simTime+8};state.deepSpacePeaceOffers.push(offer);if(offer.to===0||offer.from===0){logEvent(`${empire(offer.from).name} offered peace after the limited war objective was resolved.`,"warning");renderTransmissions();updateBadges()}}
function stxDSAcceptPeace(offer,reason="limited-war settlement"){const w=state.wars.find(x=>x.id===offer?.warId&&x.active);if(!w)return;offer.status="accepted";endWar(w,reason);galacticNews(`${empire(w.a).name.toUpperCase()} AND ${empire(w.b).name.toUpperCase()} ACCEPT PEACE`,`${w.warGoal?.label||"The limited conflict"} has been resolved. The war ends without requiring total conquest.`,"good")}
function stxDSTickPeace(){state.deepSpacePeaceOffers.forEach(o=>{if(o.status!=="pending")return;const w=state.wars.find(x=>x.id===o.warId&&x.active);if(!w){o.status="closed";return}if(o.to!==0&&o.from!==0&&state.simTime>=o.autoResolveAt)stxDSAcceptPeace(o);else if(state.simTime>=o.expiresAt)o.status="expired"})}

function stxDSAgreementBetween(kind,a,b){return(state.rivalDiplomacy?.agreements||[]).find(x=>x.active!==false&&x.kind===kind&&x.expiresAt>state.simTime&&((x.by===a&&x.with===b)||(x.by===b&&x.with===a)||(x.a===a&&x.b===b)||(x.a===b&&x.b===a)))||null}
function stxDSAddAgreement(kind,a,b,duration=150,extra={}){const record=typeof stxRDAddAgreement==="function"?stxRDAddAgreement(kind,a,b,duration,extra):{id:stxDSId("ag"),kind,by:a,with:b,createdAt:state.simTime,expiresAt:state.simTime+duration,active:true,...extra};record.bilateral=kind!=="embargo";return record}
const STX_DS_canTrade=canTrade;
canTrade=function(a,b){if(stxDSAgreementBetween("embargo",a,b))return false;return STX_DS_canTrade(a,b)||!!stxDSAgreementBetween("trade-agreement",a,b)};
function stxDSProposeAgreement(kind,enemyId){
  const e=empire(enemyId),rel=relation(0,enemyId),policy=e?.foreignPolicy||{},cost={"trade-agreement":24,"non-aggression":32,"military-access":38,"basing-rights":44,"mutual-defense":58,"sensor-accord":30}[kind]||0;if(!e||getWar(0,enemyId)||stxDSAgreementBetween(kind,0,enemyId))return false;if(empire(0).credits<cost)return showToast(`Need ${cost} credits for the diplomatic mission`);empire(0).credits-=cost;const difficulty={"trade-agreement":.34,"non-aggression":.45,"military-access":.55,"basing-rights":.62,"mutual-defense":.72,"sensor-accord":.42}[kind]||.5,fit=kind==="trade-agreement"?(policy.commercialism||.4)*.26:kind==="mutual-defense"?(policy.caution||.4)*.18:(policy.patience||.4)*.12,odds=clamp(.52+rel*.32+fit-difficulty*.28,.12,.9);if(random()<=odds){stxDSAddAgreement(kind,0,enemyId,kind==="mutual-defense"?210:160);if(typeof stxRDAddCooperation==="function")stxRDAddCooperation(0,enemyId,kind,`${kind.replaceAll("-"," ")} signed`,9);galacticNews(`${empire(0).name.toUpperCase()} AND ${e.name.toUpperCase()} SIGN AGREEMENT`,`${kind.replaceAll("-"," ").toUpperCase()} terms are now active and will shape commerce, access, and future diplomatic memory.`,"good");showToast(`${e.name} accepted the ${kind.replaceAll("-"," ")}`)}else{if(typeof stxRDAddGrievance==="function")stxRDAddGrievance(e.id,0,"failed diplomatic pressure",`The Mandate pressed for a ${kind.replaceAll("-"," ")}`,1);showToast(`${e.name} declined the proposal · ${Math.round(odds*100)}% chance`)}renderRivals();return true;
}
function stxDSImposeEmbargo(enemyId){if(getWar(0,enemyId)||stxDSAgreementBetween("embargo",0,enemyId))return;stxDSAddAgreement("embargo",0,enemyId,140,{bilateral:false});if(typeof stxRDAddGrievance==="function")stxRDAddGrievance(enemyId,0,"embargo","The Mandate imposed a commercial embargo",3);galacticNews(`${empire(0).name.toUpperCase()} EMBARGOES ${empire(enemyId).name.toUpperCase()}`,"Cross-border commercial traffic is being withdrawn from the affected corridors.","warning");renderRivals()}

function stxDSCreateFrontierIncident(base){
  if(base.type!=="military"||base.owner!==0)return;const rival=state.planets.filter(p=>p.owner!==null&&p.owner!==base.owner).map(p=>({p,d:stxDSPointDistance(p,base)})).filter(x=>x.d<1050).sort((a,b)=>a.d-b.d)[0];if(!rival)return;const incident={id:stxDSId("dsi"),kind:"frontier-base",baseId:base.id,from:rival.p.owner,to:base.owner,status:"pending",createdAt:state.simTime,expiresAt:state.simTime+55,title:`${empire(rival.p.owner).name} protests ${base.name}`,message:`The proposed military base lies only ${Math.round(rival.d)}u from ${rival.p.name} in the ${base.region}.`,autoResolveAt:null};state.deepSpaceIncidents.push(incident);logEvent(`${empire(incident.from).name} issued a frontier-base protest.`,"warning");renderTransmissions();updateBadges()}
function stxDSRespondIncident(id,action){
  const q=state.deepSpaceIncidents.find(x=>x.id===id&&x.status==="pending"),base=q&&stxDSBase(q.baseId);if(!q||!base)return;q.status=action;const rival=q.from===0?q.to:q.from,playerOwns=base.owner===0;if(action==="cancel"&&playerOwns){stxDSCancelBase(base,"Construction cancelled after a diplomatic protest");if(typeof stxRDAddCooperation==="function")stxRDAddCooperation(0,rival,"frontier concession",`Cancelled ${base.name}`,11)}else if(action==="reassure"){base.reassuredEmpireId=rival;base.reassuranceUntil=state.simTime+120;if(typeof stxRDAddCooperation==="function")stxRDAddCooperation(0,rival,"frontier reassurance",`Offered inspections and reassurance over ${base.name}`,7)}else if(action==="demand"){if(typeof stxRDAddGrievance==="function")stxRDAddGrievance(rival,0,"counter-demand","The Mandate rejected the protest and demanded rival forces withdraw",4,base.id)}else if(action==="continue"&&playerOwns){if(typeof stxRDAddGrievance==="function")stxRDAddGrievance(rival,0,"frontier base",`The Mandate continued ${base.name} despite formal protest`,4,base.id)}renderTransmissions();renderRivals();updateBadges();
}

function stxDSBestIdleFleet(owner){return state.fleets.filter(f=>f.owner===owner&&!f.destroyed&&!state.ships.some(s=>s.fleetId===f.id)&&!f.deepSpaceOperationId&&!state.deepSpaceBattles.some(b=>b.attackerFleetIds.includes(f.id)||b.defenderFleetIds.includes(f.id))).sort((a,b)=>(b.strength||0)-(a.strength||0))[0]||null}
function stxDSAIObjectiveTick(){
  state.wars.filter(w=>w.active&&w.stxLimitedWar&&w.warGoal?.status==="active").forEach(w=>{const attacker=Number.isFinite(w.aggressor)?w.aggressor:w.a,defender=attacker===w.a?w.b:w.a;if(state.ships.some(s=>s.owner===attacker&&s.warId===w.id)||state.deepSpaceOperations.some(o=>o.active!==false&&o.owner===attacker))return;const fleet=stxDSBestIdleFleet(attacker);if(!fleet)return;const goal=w.warGoal;if(goal.type==="destroy-base"||goal.type==="break-trade-network"){const base=stxDSBase(goal.targetId);if(!base||base.status!=="operational"){goal.status="achieved";return stxDSCreatePeaceOffer(w)}stxDSDispatchFleet(fleet,{key:`deep-base:${base.id}`,type:"deepBase",id:base.id,name:base.name,base,planet:state.planets.find(p=>p.id===base.sponsorPlanetId)},goal.type==="destroy-base"?"assault":"raid",{warId:w.id,stxDeepMission:goal.type==="destroy-base"?"assault":"raid"})}else if(goal.type==="blockade"){const p=state.planets.find(x=>x.id===goal.targetId);if(p)stxDSDispatchFleet(fleet,{key:`planet:${p.id}`,type:"planet",id:p.id,name:p.name,planet:p},"blockade",{warId:w.id,stxDeepMission:"blockade"})}})
}
function stxDSAIBaseTick(){
  state.empires.slice(1).forEach(e=>{if(state.simTime<(e.stxNextDeepBaseAt||0))return;e.stxNextDeepBaseAt=state.simTime+rand(125,210);if(stxDSActiveBaseCount(e.id)>=Math.min(STX_DS_MAX_BASES_PER_OWNER,1+Math.floor(owned(e.id).length/3)))return;const policy=e.foreignPolicy||{},type=(policy.aggression||0)>.62?"military":(policy.commercialism||0)>.62?"trade":(policy.caution||0)>.65?"sensor":"logistics",sponsor=owned(e.id).filter(p=>stxDSCanSponsor(p,type)).sort((a,b)=>(b.infra.shipyard+b.infra.factory)-(a.infra.shipyard+a.infra.factory))[0];if(!sponsor)return;const site=stxDSGenerateSites(sponsor,type)[0];if(site)stxDSQueueBase(sponsor,type,site,false)})
}

function stxDSTick(){
  stxDSEnsureState(false);const dt=Math.max(0,Math.min(1.2,state.simTime-stxDSLastTick));stxDSLastTick=state.simTime;stxDSTickProjects(dt);stxDSTickBases(dt);stxDSTickBattles(dt);stxDSTickOperations();stxDSTickPeace();stxDSAIObjectiveTick();stxDSAIBaseTick();state.deepSpaceIncidents.filter(q=>q.status==="pending"&&q.autoResolveAt&&state.simTime>=q.autoResolveAt).forEach(q=>q.status="acknowledged");const wrecks=state.deepSpaceBases.filter(b=>b.status==="wreck").sort((a,b)=>(b.destroyedAt||0)-(a.destroyedAt||0));if(wrecks.length>STX_DS_MAX_WRECKS){const remove=new Set(wrecks.slice(STX_DS_MAX_WRECKS).map(b=>b.id));state.deepSpaceBases=state.deepSpaceBases.filter(b=>!remove.has(b.id))}stxDSPersist();
}
const STX_DS_simulate=simulate;
simulate=function(dt){STX_DS_simulate(dt);if(state.simTime-stxDSLastTick>=.45)stxDSTick()};

const STX_DS_generateGalaxy=generateGalaxy;
generateGalaxy=function(){STX_DS_generateGalaxy();stxDSEnsureState(true);stxDSLastTick=state.simTime};
const STX_DS_loadGame=loadGame;
loadGame=function(){const ok=STX_DS_loadGame();if(ok){stxDSEnsureState(false);stxDSLastTick=state.simTime}return ok};
const STX_DS_saveGame=saveGame;
saveGame=function(notify=true){stxDSPersist();return STX_DS_saveGame(notify)};

if(typeof stxRDWarObjectiveTick==="function"){
  const STX_DS_rdWarObjectiveTick=stxRDWarObjectiveTick;
  stxRDWarObjectiveTick=function(){const wars=state.wars,limited=wars.filter(w=>w.active&&w.stxLimitedWar);state.wars=wars.filter(w=>!w.stxLimitedWar);try{STX_DS_rdWarObjectiveTick()}finally{state.wars=wars}limited.length&&stxDSAIObjectiveTick()};
}

if(typeof stxPointDetected==="function"){
  const STX_DS_pointDetected=stxPointDetected;
  stxPointDetected=function(x,y){if(STX_DS_pointDetected(x,y))return true;return stxDSBases().some(b=>b.status==="operational"&&b.type==="sensor"&&(b.owner===0||stxDSAgreementBetween("sensor-accord",0,b.owner))&&Math.hypot(b.x-x,b.y-y)<(780+(b.tier||1)*360)*b.supplyReadiness)};
}
if(typeof stxFleetPosition==="function"){
  const STX_DS_fleetPosition=stxFleetPosition;
  stxFleetPosition=function(f){const point=stxDSCurrentFleetPoint(f);if(point?.base)return{x:point.x,y:point.y,ship:null,planet:null,base:point.base,label:point.base.name};if(point?.operation)return{x:point.x,y:point.y,ship:null,planet:null,operation:point.operation,label:f.status};return STX_DS_fleetPosition(f)};
}
if(typeof stxFCAPosition==="function"){
  const STX_DS_fcaPosition=stxFCAPosition;stxFCAPosition=function(f){return stxDSCurrentFleetPoint(f)||STX_DS_fcaPosition(f)};
}
if(typeof stxFCADestinationPlanet==="function"){
  const STX_DS_fcaDestination=stxFCADestinationPlanet;stxFCADestinationPlanet=function(target){return target?.base||STX_DS_fcaDestination(target)};
}
if(typeof stxFCAConcentrationTargets==="function"){
  const STX_DS_fcaConcentrationTargets=stxFCAConcentrationTargets;
  stxFCAConcentrationTargets=function(){const base=STX_DS_fcaConcentrationTargets(),independent=stxDSBases().filter(b=>b.status==="operational"&&(b.owner===0||stxDSAgreementBetween("basing-rights",0,b.owner))).map(b=>({key:`deep-base:${b.id}`,type:"deepBase",id:b.id,name:b.name,base:b,planet:state.planets.find(p=>p.id===b.sponsorPlanetId),subtitle:`Independent ${stxDSBaseSpec(b).short.toLowerCase()} · ${b.region}`}));return[...base,...independent]};
}
if(typeof stxFCADispatchFleet==="function"){
  const STX_DS_fcaDispatch=stxFCADispatchFleet;
  stxFCADispatchFleet=function(f,destination,kind,meta={}){if(["raid","blockade"].includes(kind)||destination?.base||destination?.type==="deepBase"||f?.deepSpaceBaseId||f?.deepSpaceOperationId||state.ships.some(s=>s.fleetId===f?.id&&s.stxDeepTransit)){const ok=stxDSDispatchFleet(f,destination,kind,meta);if(ok)stxFCACommitted.add(f.id);return ok}return STX_DS_fcaDispatch(f,destination,kind,meta)};
}

globalThis.SpaceTyrantsDeepSpace={version:STX_DS_VERSION,generateSites:stxDSGenerateSites,queueBase:stxDSQueueBase,baseById:stxDSBase,canSponsor:stxDSCanSponsor,displayedIndustry:stxDSDisplayedIndustry,ensureState:stxDSEnsureState,tick:stxDSTick,dispatchFleet:stxDSDispatchFleet,destroyBase:stxDSDestroyBase,launchBonus:stxDSLaunchBonus};

function stxDSAssaultTargets(owner=0){
  const foes=new Set(state.wars.filter(w=>w.active&&(w.a===owner||w.b===owner)).map(w=>w.a===owner?w.b:w.a));
  return stxDSBases().filter(b=>b.status==="operational"&&foes.has(b.owner)).sort((a,b)=>stxDSPointDistance(a,stxDSNearestWorld(a,owner))-stxDSPointDistance(b,stxDSNearestWorld(b,owner))).map(b=>({key:`deep-base:${b.id}`,type:"deepBase",id:b.id,name:b.name,base:b,planet:state.planets.find(p=>p.id===b.sponsorPlanetId),subtitle:`ASSAULT & DESTROY · ${stxDSBaseSpec(b).short} · ${b.region}`}));
}
function stxDSRenderFCATargets(destinations,inputType,name){
  const box=$("stxFCATargetList");box.innerHTML=destinations.map(t=>`<label class="stx-fca-target ${t.type==="deepBase"?"stx-ds-target":""}"><input type="${inputType}" name="${name}" value="${stxFCAEscape(t.key)}"><span><b>${stxFCAEscape(t.name)}</b><small>${stxFCAEscape(t.subtitle||"")}</small></span></label>`).join("")||'<div class="subtle">No valid strategic target is currently available.</div>';
}
if(typeof stxFCARenderTargets==="function"){
  const STX_DS_fcaRenderTargets=stxFCARenderTargets;
  stxFCARenderTargets=function(order){
    if(!["invade","raid","blockade"].includes(order.kind))return STX_DS_fcaRenderTargets(order);stxFCAAllocations={};
    if(order.kind==="invade"){
      const planets=stxFCATargets("invade").map(p=>({key:`planet:${p.id}`,type:"planet",id:p.id,name:p.name,planet:p,subtitle:`INVADE & OCCUPY · ${empire(p.owner)?.name||"Enemy"} · ${Math.round(typeof stxWBTargetDefense==="function"?stxWBTargetDefense(p):(p.garrison||0)+(p.infra?.defense||0)*8)} defense`}));
      stxFCAUiDestinations=[...planets,...stxDSAssaultTargets(0)];return stxDSRenderFCATargets(stxFCAUiDestinations,"checkbox",`stx-fca-${order.kind}`);
    }
    stxFCAUiDestinations=stxDSEconomicTargets(order.kind,0);stxDSRenderFCATargets(stxFCAUiDestinations,"radio",`stx-fca-${order.kind}`);
  };
}
if(typeof stxFCAShow==="function"){
  const STX_DS_fcaShow=stxFCAShow;
  stxFCAShow=function(){STX_DS_fcaShow();const order=stxFCAQueue[0];if(!order||!["raid","blockade"].includes(order.kind))return;$("stxFCAKicker").textContent="WAR COMMAND · ECONOMIC WARFARE";$("stxFCATitle").textContent=order.kind==="raid"?"Raid Commerce":"Establish Blockade";$("stxFCADesc").textContent=order.kind==="raid"?"Choose a ship count and a real trade station, logistics base, or commercial corridor. Intercepted cargo will never reach its projects.":"Choose a ship count and enemy planet. The blockade works only while those fleets maintain control and intercept actual inbound and outbound shipping.";$("stxFCATargetLabel").textContent=order.kind==="raid"?"Select Commercial Target":"Select Planet to Blockade";stxFCASyncUi()};
}
if(typeof stxFCAOrderLabel==="function"){
  const STX_DS_fcaOrderLabel=stxFCAOrderLabel;
  stxFCAOrderLabel=function(kind,destination){if(destination?.base)return kind==="invade"?`Assaulting ${destination.name}`:kind==="raid"?`Raiding commerce at ${destination.name}`:`Concentrating at ${destination.name}`;if(kind==="raid")return`Raiding commerce near ${destination.name}`;if(kind==="blockade")return`Blockading ${destination.name}`;return STX_DS_fcaOrderLabel(kind,destination)};
}
if(typeof stxFCAExecuteInvasionOrder==="function"){
  stxFCAExecuteInvasionOrder=function(destinations,allocations){
    const count=destinations.reduce((n,t)=>n+(allocations[t.key]||0),0);if(!count||destinations.some(t=>{const owner=t.base?.owner??t.planet?.owner;return owner===null||owner===undefined||!empiresAtWar(0,owner)}))return 0;
    for(const target of destinations.filter(t=>!t.base))if(!stxQueueInvasion(target.planet,`Manual invasion of ${target.name}`,false))return 0;
    const chosen=stxFCAFree(destinations).slice(0,count);if(chosen.length!==count)return 0;let offset=0,moved=0;
    destinations.forEach(destination=>{const amount=allocations[destination.key]||0,ids=[];if(destination.base){for(const f of chosen.slice(offset,offset+amount)){if(stxFCADispatchFleet(f,destination,"invade",{warId:getWar(0,destination.base.owner)?.id,stxDeepMission:"assault",stxManualFleetOrder:true})){moved++;ids.push(f.id);stxFCACommitted.add(f.id)}}if(ids.length){logEvent(`BASE ASSAULT LAUNCHED: ${ids.length} ship${ids.length===1?"":"s"} committed to destroy ${destination.name}.`,"warning");stxActivity(`${ids.length} directly commanded ship${ids.length===1?"":"s"} launched toward ${destination.name}.`,destination.planet?.id||null,null,"warning")}}
      else{const plan=stxFCAInvasionPlan(destination.planet),war=getWar(0,destination.planet.owner);for(const f of chosen.slice(offset,offset+amount)){if(stxFCADispatchFleet(f,destination,"invade",{warId:war?.id,invasionPlanId:plan?.id,stxManualFleetOrder:true})){moved++;ids.push(f.id)}}if(plan&&ids.length){plan.stxManualAllocation=true;plan.stxRequestedFleetCount=ids.length;plan.stxAssignedFleetIds=ids;plan.stxManualLaunchedAt=state.simTime;plan.lastLaunchAt=state.simTime;plan.status="fleet inbound"}if(ids.length){logEvent(`INVASION LAUNCHED: ${ids.length} ship${ids.length===1?"":"s"} committed to ${destination.name}.`,"warning");stxActivity(`${ids.length} directly commanded ship${ids.length===1?"":"s"} launched for ${destination.name}.`,destination.planet.id,null,"warning")}}offset+=amount});
    if(moved===count&&typeof stxRefreshFleetLocator==="function")stxRefreshFleetLocator();return moved;
  };
}
function stxDSExecuteEconomicOrder(kind,destination,count){
  if(!destination||count<1)return 0;const owner=destination.base?.owner??destination.planet?.owner;if(owner==null||!empiresAtWar(0,owner))return 0;const chosen=stxFCAFree([destination]).slice(0,count);if(chosen.length!==count)return 0;let moved=0;chosen.forEach(f=>{if(stxFCADispatchFleet(f,destination,kind,{warId:getWar(0,owner)?.id,stxDeepMission:kind,stxManualFleetOrder:true})){moved++;stxFCACommitted.add(f.id)}});if(moved===count){logEvent(`${kind==="blockade"?"BLOCKADE":"COMMERCE RAID"} ORDER: ${moved} ship${moved===1?"":"s"} assigned to ${destination.name}.`,"warning");if(typeof stxRefreshFleetLocator==="function")stxRefreshFleetLocator()}return moved;
}
if(typeof stxFCAConfirm==="function"){
  const STX_DS_fcaConfirm=stxFCAConfirm;
  stxFCAConfirm=function(){const order=stxFCAQueue[0];if(!order||!["raid","blockade"].includes(order.kind))return STX_DS_fcaConfirm();const selected=stxFCASelectedDestinations(),n=stxFCACount(),moved=selected[0]?stxDSExecuteEconomicOrder(order.kind,selected[0],n):0;if(moved!==n||n<1)return showToast("The exact economic-warfare order could not be completed");const direct=stxFCADirect;stxFCAQueue.shift();saveGame(false);stxFCAQueue.length?stxFCAShow():stxFCAFinish(direct)};
}
COMMANDS.push(
  {id:"deepCommerceRaid",cat:"War",title:"Raid Commerce",desc:"Choose fleets and physically patrol a hostile trade station, logistics base, or commercial corridor.",effects:["Intercept real freight","Projects lose undelivered cargo","Merchants reroute"],score:()=>stxFCAWars().length&&stxDSEconomicTargets("raid",0).length?82:0,target:()=>stxDSEconomicTargets("raid",0)[0]?.planet||playerWorlds()[0],apply:()=>{stxFCAQueue.push({kind:"raid"});return true}},
  {id:"deepBlockade",cat:"War",title:"Establish Blockade",desc:"Choose fleets and maintain control around an enemy world to stop actual imports and exports.",effects:["Blocks inbound freight","Delays shipbuilding and projects","Ends if fleet control is lost"],score:()=>stxFCAWars().length&&stxDSEconomicTargets("blockade",0).length?78:0,target:()=>stxDSEconomicTargets("blockade",0)[0]?.planet||playerWorlds()[0],apply:()=>{stxFCAQueue.push({kind:"blockade"});return true}}
);

function stxDSPreselectFleetTarget(kind,key){
  if(typeof stxFCAOpenDirect!=="function")return;stxFCAOpenDirect(kind);const input=[...($("stxFCATargetList")?.querySelectorAll('input[type="radio"]')||[])].find(node=>node.value===key);if(input){input.checked=true;input.dispatchEvent(new Event("change",{bubbles:true}))}
}
function stxDSProjectMaterials(base){const q=base.project;return Object.entries(q?.need||{}).map(([r,a])=>{const got=q.delivered?.[r]||0,incoming=stxDSProjectIncoming(base,r);return`<span class="${got>=a-.01?"ready":""}">${stxDSEscape(RESOURCE_LABEL[r]||r)} ${Math.floor(got)}/${a}${incoming?` · ${Math.ceil(incoming)} inbound`:""}</span>`}).join("")}
function stxDSProjectCard(base){
  const q=base.project,ships=state.ships.filter(s=>s.stxDeepTransit&&s.deepBaseId===base.id),ratio=Math.round(stxDSProjectRatio(base)*100);return`<article class="project-row active stx-ds-project"><div class="stx-ol-phase">${stxDSEscape(q.phase).toUpperCase()} · ${stxDSEscape(base.region)}</div><div class="project-head"><strong>${stxDSEscape(base.name)}</strong><b>${Math.round(q.progress*100)}%</b></div><div class="project-desc">Independent ${stxDSEscape(stxDSBaseSpec(base).short.toLowerCase())}. ${ratio}% of required material has physically reached the deep-space construction site.</div><div class="stx-ds-materials">${stxDSProjectMaterials(base)}</div><div class="project-track"><i style="width:${Math.round(q.progress*100)}%"></i></div>${ships.map(s=>`<button class="stx-ds-transit" data-ds-focus-ship="${s.id}"><b>${stxDSEscape(s.vesselName)}</b><span>${fmtEta(Math.max(0,(1-s.progress)*s.distance/s.speed))} · ${Object.entries(s.cargo||{}).map(([r,v])=>`${Math.round(v)} ${RESOURCE_LABEL[r]||r}`).join(", ")}</span></button>`).join("")||'<div class="subtle">Sponsor stockpiles are assembling the next construction shipment.</div>'}</article>`
}
function stxDSPlanetConstructionBlock(p){
  const projects=stxDSBases(p.owner).filter(b=>b.sponsorPlanetId===p.id&&b.status==="construction"),canBuild=p.owner===0&&(p.infra?.factory||0)>=1&&(p.infra?.shipyard||0)>=1,types=Object.keys(STX_DS_BASE_TYPES),industry=stxDSDisplayedIndustry(p.owner);if(!projects.length&&!canBuild)return"";
  return`<section class="stx-ds-planet-block"><div class="section-label">Independent Deep-Space Infrastructure</div>${projects.map(stxDSProjectCard).join("")}${canBuild?`<div class="stx-ds-build-grid">${types.map(type=>{const spec=stxDSBaseSpec(type),militaryLocked=type==="military"&&industry<STX_DS_MILITARY_INDUSTRY_UNLOCK;return`<button class="choice-btn" data-ds-build="${type}" ${stxDSCanSponsor(p,type)?"":"disabled"}${militaryLocked?` title="Requires Industry ${STX_DS_MILITARY_INDUSTRY_UNLOCK.toFixed(1)} or higher"`:""}><b>${spec.short}</b><small>${militaryLocked?`LOCKED · Industry ${industry.toFixed(1)} / ${STX_DS_MILITARY_INDUSTRY_UNLOCK.toFixed(1)}`:type==="military"?"Frontier staging":type==="trade"?"Route intersection":type==="logistics"?"Long-haul relay":"Frontier detection"}</small></button>`}).join("")}</div><p class="subtle">Military bases unlock when the empire-wide Industry number in the top bar reaches ${STX_DS_MILITARY_INDUSTRY_UNLOCK.toFixed(1)}. Choose from three automatically generated strategic sites.</p>`:""}</section>`;
}
function stxDSSupplyLabel(base){const n=base.supplyReadiness;return n>=.75?"Full capability":n>=.4?"Reduced service":n>=.15?"Emergency operations":"Minimal capability"}
function stxDSBasePanel(base){
  const spec=stxDSBaseSpec(base),owner=empire(base.owner),p=state.planets.find(x=>x.id===base.sponsorPlanetId),health=Math.round(base.hp/base.maxHp*100),supply=Math.round(base.supplyReadiness*100),docked=base.dockedFleetIds.map(fleetRecord).filter(Boolean),incoming=state.ships.filter(s=>s.stxDeepTransit&&s.deepBaseId===base.id).slice(0,6),bonus=stxDSLaunchBonus(base),cycles=f=>Math.max(0,Math.ceil((f.preparedUntil-state.simTime)/Math.max(1,state.commandCycle||45)));
  if(base.status==="wreck")return`<section class="stx-ds-base-panel wreck" style="--ds-color:${spec.color}"><div class="stx-ol-phase">WRECK · ${stxDSEscape(base.region)}</div><h3>Wreck of ${stxDSEscape(base.name)}</h3><p class="subtle">${stxDSEscape(base.destroyedReason||"Destroyed strategic infrastructure")}. Ownership did not transfer; all regional benefits ended immediately.</p></section>`;
  if(base.status==="construction")return`<section class="stx-ds-base-panel" style="--ds-color:${spec.color}"><div class="stx-ol-phase">DEEP-SPACE CONSTRUCTION SITE · ${stxDSEscape(base.region)}</div><h3>${stxDSEscape(base.name)}</h3>${stxDSProjectCard(base)}</section>`;
  const canUse=base.owner===0||!!stxDSAgreementBetween("basing-rights",0,base.owner),enemy=base.owner!==0&&empiresAtWar(0,base.owner);
  return`<section class="stx-ds-base-panel" style="--ds-color:${spec.color}"><div class="stx-ds-base-head"><div><div class="stx-ol-phase">INDEPENDENT ${spec.short} · ${stxDSEscape(base.region)}</div><h3>${stxDSEscape(base.name)}</h3><small>${stxDSEscape(owner?.name||"Unknown")} · sponsored by ${stxDSEscape(p?.name||"unknown world")}</small></div><b>${health}% INTEGRITY</b></div><div class="stx-ol-stat-grid"><span><label>Supply readiness</label><b>${supply}%</b></span><span><label>Capability</label><b>${stxDSSupplyLabel(base)}</b></span><span><label>Docked fleets</label><b>${docked.length}</b></span><span><label>Travel launch</label><b>+${Math.round(bonus.speed*100)}%</b></span><span><label>Coordination</label><b>+${Math.round(bonus.coordination*100)}%</b></span><span><label>Throughput</label><b>${Math.round(base.tradeThroughput)}</b></span></div><div class="choice-row">${canUse?`<button class="choice-btn primary-choice" data-ds-command="concentrate" data-ds-target="${base.id}">Concentrate Fleets Here</button>`:""}${enemy?`<button class="choice-btn danger-choice" data-ds-command="invade" data-ds-target="${base.id}">Assault & Destroy</button>`:""}${enemy&&["trade","logistics"].includes(base.type)?`<button class="choice-btn danger-choice" data-ds-command="raid" data-ds-target="${base.id}">Raid Commerce</button>`:""}</div><div class="stx-ol-columns"><div><div class="section-label">Docked / Servicing</div>${docked.map(f=>`<div class="stx-ol-list"><b>${stxDSEscape(f.name)}</b><span>${stxDSEscape(stxDSFleetType(f))} · ${f.preparedUntil>state.simTime?`BASE PREPARED ${cycles(f)} cycles`:`${stxDSEscape(f.readiness)}`}</span></div>`).join("")||'<div class="subtle">No fleet is docked.</div>'}</div><div><div class="section-label">Inbound Supply & Traffic</div>${incoming.map(s=>`<button class="stx-ds-transit" data-ds-focus-ship="${s.id}"><b>${stxDSEscape(s.vesselName)}</b><span>${stxDSEscape(s.stxDeepMission)} · ${fmtEta(Math.max(0,(1-s.progress)*s.distance/s.speed))}</span></button>`).join("")||'<div class="subtle">No tracked arrivals.</div>'}</div></div><div class="section-label">Recent Activity</div>${base.activity.slice(0,5).map(a=>`<div class="stx-ol-list"><b>${stxDSEscape(a.text)}</b><span>${fmtEta(Math.max(0,state.simTime-a.time))} ago</span></div>`).join("")||'<div class="subtle">No recent operations.</div>'}</section>`;
}
function stxDSEconomicPressureBlock(p){
  const ops=state.deepSpaceOperations.filter(o=>o.active!==false&&o.targetType==="planet"&&o.targetId===p.id),hostile=ops.filter(o=>o.owner!==p.owner),ours=ops.filter(o=>o.owner===0);if(!ops.length)return"";const intercepted=ops.reduce((n,o)=>n+o.intercepted,0),rerouted=ops.reduce((n,o)=>n+o.rerouted,0),waiting=(p.orders||[]).filter(o=>o.status==="waiting").length,enemy=hostile[0]&&empire(hostile[0].owner);
  return`<section class="stx-ds-pressure ${hostile.length?"hostile":""}"><div class="section-label">${hostile.length?"ECONOMIC WARFARE UNDERWAY":"OUR ECONOMIC WARFARE"}</div><div class="project-head"><strong>${hostile.some(o=>o.kind==="blockade")?"ACTIVE BLOCKADE":"COMMERCE RAIDING"}</strong><b>${intercepted} intercepted</b></div><div class="project-desc">${enemy?`${stxDSEscape(enemy.name)} fleets are disrupting real shipping.`:"Mandate fleets are disrupting hostile shipping."} ${rerouted} vessel${rerouted===1?" has":"s have"} rerouted; ${waiting} local order${waiting===1?" is":"s are"} still waiting. Construction and shipbuilding slow only when needed cargo fails to arrive.</div></section>`;
}
function stxDSBindPlanetActions(p,body){
  body.querySelectorAll("[data-ds-build]").forEach(b=>b.onclick=()=>stxDSOpenPlacement(p,b.dataset.dsBuild));
  body.querySelectorAll("[data-ds-focus-ship]").forEach(b=>b.onclick=()=>{const s=state.ships.find(x=>x.id===b.dataset.dsFocusShip);if(s){stxFocusPoint(s.x,s.y,1.25);showToast(`${s.vesselName} · ${Math.round(s.progress*100)}% of route complete`)}});body.querySelectorAll("[data-ds-command]").forEach(b=>{b.onclick=()=>stxDSPreselectFleetTarget(b.dataset.dsCommand,`deep-base:${b.dataset.dsTarget}`)});
}
const STX_DS_renderPlanet=renderPlanet;
renderPlanet=function(){
  STX_DS_renderPlanet();const p=state.selected,body=$("planetBody");if(!p||!body)return;const selected=stxDSBase(state.stxSelectedDeepBaseId);if(selected&&selected.sponsorPlanetId!==p.id&&stxDSPointDistance(selected,p)>260)state.stxSelectedDeepBaseId=null;else if(selected)body.insertAdjacentHTML("afterbegin",stxDSBasePanel(selected));body.insertAdjacentHTML("beforeend",stxDSEconomicPressureBlock(p)+stxDSPlanetConstructionBlock(p));stxDSBindPlanetActions(p,body);
};

function stxDSOpenPlacement(p,type){
  const modal=$("stxDSPlacementModal");if(!modal||!stxDSCanSponsor(p,type))return showToast("This planet cannot sponsor that facility right now");const sites=stxDSGenerateSites(p,type),spec=stxDSBaseSpec(type);stxDSPlacement={planetId:p.id,type,sites};$("stxDSPlacementKicker").textContent=`${p.name.toUpperCase()} · ${spec.short}`;$("stxDSPlacementTitle").textContent="Choose a Strategic Site";$("stxDSPlacementCopy").textContent="The planning staff generated three sensible positions. Choose the strategic purpose; exact placement is automatic.";$("stxDSPlacementSites").innerHTML=sites.map((s,i)=>`<button class="stx-ds-site" data-ds-site="${i}"><b>${stxDSEscape(s.name)}</b><span>${stxDSEscape(s.region)} · ${s.distance}u from ${stxDSEscape(p.name)}</span><small>${stxDSEscape(s.reason)}</small></button>`).join("");modal.hidden=false;$("stxDSPlacementSites").querySelectorAll("[data-ds-site]").forEach(b=>b.onclick=()=>{const site=sites[Number(b.dataset.dsSite)];if(stxDSQueueBase(p,type,site,true)){modal.hidden=true;state.stxSelectedDeepBaseId=state.deepSpaceBases[state.deepSpaceBases.length-1].id;renderPlanet();updateHud(true);showToast(`${site.name} authorized in the ${site.region}`)}})
}
function stxDSInstallPlacementUI(){
  if($("stxDSPlacementModal"))return;const modal=document.createElement("div");modal.id="stxDSPlacementModal";modal.className="modal-wrap";modal.hidden=true;modal.innerHTML=`<section class="glass stx-ds-placement" role="dialog" aria-modal="true"><div class="stx-fca-head"><div><div class="kicker" id="stxDSPlacementKicker"></div><h2 id="stxDSPlacementTitle"></h2></div><button class="choice-btn" id="stxDSPlacementClose">Back</button></div><p class="subtle" id="stxDSPlacementCopy"></p><div id="stxDSPlacementSites"></div></section>`;document.body.appendChild(modal);$("stxDSPlacementClose").onclick=()=>modal.hidden=true;
}

function stxDSIncidentCard(q){
  const base=stxDSBase(q.baseId);if(!base)return"";return`<article class="transmission-card urgent stx-ds-incident"><div class="card-kicker">FRONTIER BASE PROTEST // ${stxDSEscape(empire(q.from)?.name)}</div><div class="card-title"><strong>${stxDSEscape(q.title)}</strong><span class="news-time">${fmtEta(q.expiresAt-state.simTime)}</span></div><p class="card-copy">${stxDSEscape(q.message)}</p><div class="choice-row"><button class="choice-btn danger-choice" data-ds-incident="${q.id}" data-ds-response="continue">Continue Construction</button><button class="choice-btn primary-choice" data-ds-incident="${q.id}" data-ds-response="reassure">Offer Reassurance</button><button class="choice-btn" data-ds-incident="${q.id}" data-ds-response="cancel">Cancel Project</button><button class="choice-btn" data-ds-incident="${q.id}" data-ds-response="demand">Demand Their Withdrawal</button></div></article>`;
}
function stxDSPeaceCard(o){
  const w=state.wars.find(x=>x.id===o.warId&&x.active);if(!w)return"";return`<article class="transmission-card urgent stx-ds-peace"><div class="card-kicker">LIMITED WAR SETTLEMENT // ${stxDSEscape(empire(o.from)?.name)}</div><div class="card-title"><strong>Peace after ${stxDSEscape(o.goal)}</strong><span class="news-time">${fmtEta(o.expiresAt-state.simTime)}</span></div><p class="card-copy">The stated war objective has been resolved. Peace can now end the conflict without total planetary conquest.</p><div class="choice-row"><button class="choice-btn primary-choice" data-ds-peace="${o.id}" data-ds-peace-action="accept">Accept Status Quo</button><button class="choice-btn danger-choice" data-ds-peace="${o.id}" data-ds-peace-action="continue">Continue the War</button></div></article>`;
}
const STX_DS_renderTransmissions=renderTransmissions;
renderTransmissions=function(){
  STX_DS_renderTransmissions();const box=$("transmissionList");if(!box)return;const incidents=state.deepSpaceIncidents.filter(q=>q.status==="pending"&&q.to===0),peace=state.deepSpacePeaceOffers.filter(o=>o.status==="pending"&&(o.to===0||o.from===0));if((incidents.length||peace.length)&&box.querySelector(".empty-hub"))box.innerHTML="";box.insertAdjacentHTML("beforeend",incidents.map(stxDSIncidentCard).join("")+peace.map(stxDSPeaceCard).join(""));box.querySelectorAll("[data-ds-incident]").forEach(b=>b.onclick=()=>stxDSRespondIncident(b.dataset.dsIncident,b.dataset.dsResponse));box.querySelectorAll("[data-ds-peace]").forEach(b=>b.onclick=()=>{const o=state.deepSpacePeaceOffers.find(x=>x.id===b.dataset.dsPeace);if(!o)return;if(b.dataset.dsPeaceAction==="accept")stxDSAcceptPeace(o);else{const w=state.wars.find(x=>x.id===o.warId);o.status="declined";if(w){w.stxLimitedWar=false;w.warGoal.status="escalated"}showToast("The limited peace offer was refused")}renderTransmissions();renderRivals();updateBadges()});
};
const STX_DS_updateBadges=updateBadges;
updateBadges=function(){STX_DS_updateBadges();const m=$("messageCount");if(!m)return;const extra=state.deepSpaceIncidents.filter(q=>q.status==="pending"&&q.to===0).length+state.deepSpacePeaceOffers.filter(o=>o.status==="pending"&&(o.to===0||o.from===0)).length;if(extra){m.textContent=Math.min(99,(Number(m.textContent)||0)+extra);m.hidden=false}};

function stxDSAgreementLabel(kind){return({"trade-agreement":"Trade Agreement","non-aggression":"Non-Aggression","military-access":"Military Access","basing-rights":"Basing Rights","mutual-defense":"Mutual Defense","sensor-accord":"Sensor Accord",embargo:"Embargo"})[kind]||kind}
function stxDSDiplomaticMemories(enemyId){
  const pair=typeof stxRDPair==="function"?stxRDPair(0,enemyId):null,items=[...(pair?.grievances||[]).filter(g=>g.holder===enemyId||g.against===enemyId).map(g=>({text:g.text,value:-Math.round(g.weight||5),time:g.time})),...(pair?.cooperation||[]).map(c=>({text:c.text,value:Math.round(c.value||5),time:c.time}))].sort((a,b)=>b.time-a.time).slice(0,4);return items;
}
function stxDSDiplomacyPanel(enemyId){
  const e=empire(enemyId),war=getWar(0,enemyId),agreements=(state.rivalDiplomacy?.agreements||[]).filter(a=>a.active!==false&&a.expiresAt>state.simTime&&((a.by===0&&a.with===enemyId)||(a.by===enemyId&&a.with===0))),memories=stxDSDiplomaticMemories(enemyId),goal=war?.warGoal,baseGoal=goal&&stxDSBase(goal.targetId),picker=state.stxDSAgreementPickerFor===enemyId;
  return`<section class="stx-ds-diplomacy"><div class="stx-ol-phase">LIVING RELATIONSHIP · ${stxDSEscape(e.name)}</div>${agreements.length?`<div class="stx-ds-agreement-tags">${agreements.map(a=>`<span class="${a.kind==="embargo"?"hostile":""}">${stxDSEscape(stxDSAgreementLabel(a.kind))}</span>`).join("")}</div>`:'<div class="subtle">No active agreements.</div>'}${goal?`<div class="stx-ds-war-goal"><label>${stxDSEscape(e.name)} WAR GOAL</label><b>${stxDSEscape(goal.label)}</b><span>${goal.status.toUpperCase()}${goal.limited?" · LIMITED WAR":""}</span></div>`:""}${memories.length?`<div class="section-label">Recent relationship factors</div>${memories.map(m=>`<div class="stx-ds-memory"><span>${stxDSEscape(m.text)}</span><b class="${m.value>=0?"good":"bad"}">${m.value>=0?"+":""}${m.value}</b></div>`).join("")}`:""}<div class="choice-row">${!war?`<button class="choice-btn" data-ds-diplomacy="improve" data-ds-rival="${enemyId}">Improve Relations · 22 cr</button><button class="choice-btn" data-ds-diplomacy="agreements" data-ds-rival="${enemyId}">Make Agreement</button><button class="choice-btn danger-choice" data-ds-diplomacy="embargo" data-ds-rival="${enemyId}">Embargo</button>`:`<button class="choice-btn primary-choice" data-ds-diplomacy="statusquo" data-ds-rival="${enemyId}">Offer Status Quo Peace</button>${baseGoal&&baseGoal.owner===0?`<button class="choice-btn" data-ds-diplomacy="dismantle" data-ds-rival="${enemyId}">Dismantle ${stxDSEscape(baseGoal.name)} for Peace</button>`:""}`}</div>${picker?`<div class="stx-ds-agreement-grid">${["trade-agreement","non-aggression","military-access","basing-rights","mutual-defense","sensor-accord"].map(k=>`<button class="choice-btn" data-ds-agreement="${k}" data-ds-rival="${enemyId}">${stxDSEscape(stxDSAgreementLabel(k))}</button>`).join("")}</div>`:""}</section>`;
}
function stxDSHandleDiplomacy(action,enemyId){
  const e=empire(enemyId),war=getWar(0,enemyId);if(!e)return;if(action==="improve"){if(empire(0).credits<22)return showToast("Need 22 credits");empire(0).credits-=22;if(typeof stxRDAddCooperation==="function")stxRDAddCooperation(0,enemyId,"diplomatic mission","A sustained diplomatic mission improved direct contacts",8);showToast(`Relations with ${e.name} improved`)}else if(action==="agreements"){state.stxDSAgreementPickerFor=state.stxDSAgreementPickerFor===enemyId?null:enemyId}else if(action==="embargo")stxDSImposeEmbargo(enemyId);else if(action==="statusquo"&&war){const offer={warId:war.id,status:"pending"};stxDSAcceptPeace(offer,"status quo")}else if(action==="dismantle"&&war){const base=stxDSBase(war.warGoal?.targetId);if(base&&base.owner===0){stxDSCancelBase(base,"Dismantled under a limited-war peace settlement");galacticNews(`${base.name.toUpperCase()} DISMANTLED`,`${empire(0).name} removed the frontier facility under a negotiated peace settlement.`,"warning",base.sponsorPlanetId);stxDSAcceptPeace({warId:war.id,status:"pending"},"demilitarized-frontier settlement")}}renderRivals();updateHud(true);
}
function stxDSDecorateRivals(){
  const grid=$("rivalGrid");if(!grid||!state.empires?.length)return;let panel=$("stxDSDiplomacyPanel");if(!panel){panel=document.createElement("div");panel.id="stxDSDiplomacyPanel";const anchor=$("stxRDDiplomaticActions")||$("stxOlRivalActions");anchor?anchor.before(panel):grid.after(panel)}const id=Number.isFinite(state.stxSelectedRivalId)?state.stxSelectedRivalId:state.empires[1]?.id;if(!empire(id))return;panel.innerHTML=stxDSDiplomacyPanel(id);panel.querySelectorAll("[data-ds-diplomacy]").forEach(b=>b.onclick=()=>stxDSHandleDiplomacy(b.dataset.dsDiplomacy,Number(b.dataset.dsRival)));panel.querySelectorAll("[data-ds-agreement]").forEach(b=>b.onclick=()=>stxDSProposeAgreement(b.dataset.dsAgreement,Number(b.dataset.dsRival)));
}
const STX_DS_renderRivals=renderRivals;
renderRivals=function(){STX_DS_renderRivals();stxDSDecorateRivals()};

function stxDSSelectBaseAtEvent(e){
  if(typeof drag!=="undefined"&&drag?.moved)return;const w=screenToWorld(e.clientX,e.clientY),hit=stxDSBases().filter(b=>visible(b.x,b.y,90)&&Math.hypot(b.x-w.x,b.y-w.y)<Math.max(22,30/state.camera.zoom)).sort((a,b)=>Math.hypot(a.x-w.x,a.y-w.y)-Math.hypot(b.x-w.x,b.y-w.y))[0];if(!hit)return;e.preventDefault();e.stopImmediatePropagation();state.stxSelectedDeepBaseId=hit.id;state.stxSelectedFacilityId=null;state.selected=state.planets.find(p=>p.id===hit.sponsorPlanetId)||state.selected;stxFocusPoint(hit.x,hit.y,1.12);renderPlanet();$("leftPanel")?.classList.add("open");showToast(`${hit.name} · ${hit.status==="wreck"?"WRECK":`${Math.round(hit.supplyReadiness*100)}% supplied`}`);
}

stxDSEnsureState(false);stxDSInstallPlacementUI();canvas.addEventListener("pointerup",stxDSSelectBaseAtEvent,true);

function stxDSHash(text){let h=2166136261;for(const c of String(text)){h^=c.charCodeAt(0);h=Math.imul(h,16777619)}return h>>>0}
function stxDSDrawTransitShip(s){
  if(!visible(s.x,s.y,45)||(s.type==="fleet"&&state.camera.zoom>=.46))return;const p=worldToScreen(s.x,s.y),a=Math.atan2(s.targetY-s.y,s.targetX-s.x),color=s.type==="fleet"?(empire(s.owner)?.color||"#fff"):s.type==="supply"?"#75eaff":s.type==="construction"?"#72f4e8":"#ffd56f",z=state.camera.zoom,size=clamp((s.type==="fleet"?4.2:3)*z,2.2,7);ctx.save();ctx.translate(p.x,p.y);ctx.rotate(a);ctx.fillStyle=color;ctx.strokeStyle=color;ctx.shadowColor=color;ctx.shadowBlur=s.type==="fleet"?14:8;if(s.type==="construction"){ctx.strokeRect(-size*1.3,-size*.7,size*2.5,size*1.4);ctx.beginPath();ctx.moveTo(size*.25,-size*.7);ctx.lineTo(size*1.8,-size*1.35);ctx.moveTo(size*.25,size*.7);ctx.lineTo(size*1.8,size*1.35);ctx.stroke()}else if(s.type==="supply"){ctx.fillRect(-size*1.45,-size*.65,size*2.9,size*1.3);ctx.fillStyle="#fff";ctx.globalAlpha=.7;ctx.fillRect(-size*.14,-size*.48,size*.28,size*.96)}else{ctx.beginPath();ctx.moveTo(size*2,0);ctx.lineTo(-size,-size);ctx.lineTo(-size*.3,0);ctx.lineTo(-size,size);ctx.closePath();ctx.fill()}if(s.stxRerouted){ctx.strokeStyle="#ffca6f";ctx.setLineDash([2,3]);ctx.beginPath();ctx.moveTo(-size*1.2,0);ctx.lineTo(-size*4.2,0);ctx.stroke()}ctx.restore();state.stats.visible++;
}
const STX_DS_drawShips=drawShips;
drawShips=function(){const all=state.ships,special=all.filter(s=>s.stxDeepTransit),regular=all.filter(s=>!s.stxDeepTransit);state.ships=regular;try{STX_DS_drawShips()}finally{state.ships=all}special.forEach(stxDSDrawTransitShip)};

function stxDSDrawBase(base){
  if(!visible(base.x,base.y,130))return;const spec=stxDSBaseSpec(base),s=worldToScreen(base.x,base.y),z=state.camera.zoom,size=clamp((5.5+(base.tier||1)*1.6)*Math.max(.62,z),4,18),selected=state.stxSelectedDeepBaseId===base.id,t=performance.now()/1000;ctx.save();
  if(selected){ctx.strokeStyle=spec.color;ctx.globalAlpha=.34;ctx.setLineDash([5,7]);ctx.beginPath();ctx.arc(s.x,s.y,Math.max(26,220*z),0,6.283);ctx.stroke();ctx.setLineDash([]);ctx.globalAlpha=1;ctx.shadowColor=spec.color;ctx.shadowBlur=20}
  ctx.translate(s.x,s.y);ctx.rotate(t*.055+(stxDSHash(base.id)%100)*.01);ctx.strokeStyle=base.status==="wreck"?"#ff6178":spec.color;ctx.fillStyle="rgba(5,12,25,.96)";ctx.lineWidth=selected?2:1.2;ctx.shadowColor=ctx.strokeStyle;ctx.shadowBlur=base.status==="wreck"?5:11;
  if(base.status==="wreck"){ctx.globalAlpha=.58;ctx.beginPath();ctx.moveTo(-size*1.4,-size*.6);ctx.lineTo(-size*.2,-size*.1);ctx.lineTo(size*.55,-size);ctx.moveTo(-size*.7,size*.9);ctx.lineTo(size*.2,size*.15);ctx.lineTo(size*1.35,size*.55);ctx.stroke();for(let i=0;i<5;i++){const a=t*.03+i*1.25,r=size*(1.1+i*.18);ctx.fillStyle=i%2?"#ff8a72":"#78869b";ctx.fillRect(Math.cos(a)*r-1,Math.sin(a)*r*.7-1,2+i%2,2)}}else if(base.status==="construction"){const progress=base.project?.progress||0;ctx.globalAlpha=.4+progress*.6;ctx.setLineDash([3,3]);ctx.strokeRect(-size,-size*.65,size*2,size*1.3);ctx.setLineDash([]);ctx.beginPath();ctx.arc(0,0,size*.7,-1.57,-1.57+6.283*progress);ctx.stroke();for(let i=0;i<3;i++){const a=t*.7+i*2.1;ctx.fillStyle=i===0?"#fff":spec.color;ctx.fillRect(Math.cos(a)*size*1.5-1,Math.sin(a)*size*.8-1,3,2)}}else if(base.type==="military"){ctx.beginPath();ctx.moveTo(0,-size*1.35);ctx.lineTo(size,-size*.35);ctx.lineTo(size*.72,size);ctx.lineTo(0,size*.55);ctx.lineTo(-size*.72,size);ctx.lineTo(-size,-size*.35);ctx.closePath();ctx.fill();ctx.stroke();ctx.beginPath();ctx.moveTo(-size*1.35,0);ctx.lineTo(size*1.35,0);ctx.moveTo(0,-size*1.65);ctx.lineTo(0,size*1.4);ctx.stroke()}else if(base.type==="trade"){ctx.beginPath();ctx.moveTo(-size*1.25,0);ctx.lineTo(-size*.55,-size*.75);ctx.lineTo(size*.6,-size*.75);ctx.lineTo(size*1.25,0);ctx.lineTo(size*.6,size*.75);ctx.lineTo(-size*.55,size*.75);ctx.closePath();ctx.fill();ctx.stroke();ctx.beginPath();ctx.ellipse(0,0,size*1.2,size*.36,0,0,6.283);ctx.stroke()}else if(base.type==="sensor"){ctx.beginPath();ctx.arc(0,0,size*.62,0,6.283);ctx.fill();ctx.stroke();ctx.beginPath();ctx.arc(0,0,size*1.35,-2.6,-.55);ctx.stroke();ctx.moveTo(0,0);ctx.lineTo(size*1.3,-size*.65);ctx.stroke()}else{ctx.fillRect(-size,-size*.45,size*2,size*.9);ctx.strokeRect(-size,-size*.45,size*2,size*.9);ctx.beginPath();ctx.arc(0,0,size*.9,0,6.283);ctx.stroke();ctx.moveTo(-size*1.5,0);ctx.lineTo(size*1.5,0);ctx.stroke()}
  if(base.status==="operational"&&base.supplyReadiness<.4){ctx.strokeStyle="#ffb15f";ctx.globalAlpha=.5+.4*Math.sin(t*5);ctx.beginPath();ctx.arc(0,0,size*1.7,0,6.283);ctx.stroke()}ctx.restore();
  if(base.status==="operational"&&z>.62){const fleets=base.dockedFleetIds.map(fleetRecord).filter(Boolean).slice(0,STX_DS_VISIBLE_TRAFFIC);fleets.forEach((f,i)=>{const a=t*(i%2?-.15:.12)+i*1.7,r=(size*2.1+i*2),p={x:s.x+Math.cos(a)*r,y:s.y+Math.sin(a)*r*.56},target={x:p.x+Math.cos(a+1.57)*9,y:p.y+Math.sin(a+1.57)*9};drawCombatCraft(p,target,empire(f.owner)?.color||"#fff",clamp(2*z,1.7,4),i===0&&f.strength>35)})}
  if(z>.48){ctx.save();ctx.textAlign="left";ctx.font=`800 ${Math.max(7,8*z)}px system-ui`;ctx.fillStyle=base.status==="wreck"?"#ff778a":spec.color;ctx.fillText(`${base.status==="wreck"?"WRECK OF ":""}${base.name.toUpperCase()}`,s.x+size+8,s.y-size-3);if(z>.72){ctx.fillStyle="#8197ad";ctx.font="700 7px system-ui";ctx.fillText(`${base.region} · ${base.status==="operational"?`${Math.round(base.supplyReadiness*100)}% SUPPLY`:String(base.status).toUpperCase()}`,s.x+size+8,s.y+7)}ctx.restore()}state.stats.visible++;
}
function stxDSDrawNetworks(){
  const mode=state.stxNetworkOverlay,t=performance.now()/1000;ctx.save();ctx.lineWidth=1.15;
  if(mode==="logistics")stxDSBases().filter(b=>b.status!=="wreck").forEach(b=>{const p=state.planets.find(x=>x.id===b.sponsorPlanetId);if(!p)return;const a=worldToScreen(p.x,p.y),c=worldToScreen(b.x,b.y);ctx.strokeStyle=`rgba(105,239,255,${.16+b.supplyReadiness*.34})`;ctx.setLineDash([5,7]);ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(c.x,c.y);ctx.stroke()});
  if(mode==="trade")stxDSBases().filter(b=>b.status==="operational"&&b.type==="trade").forEach(b=>{state.planets.slice().sort((a,c)=>stxDSPointDistance(a,b)-stxDSPointDistance(c,b)).slice(0,3).forEach(p=>{const a=worldToScreen(p.x,p.y),c=worldToScreen(b.x,b.y);ctx.strokeStyle="rgba(255,213,111,.28)";ctx.setLineDash([3,8]);ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(c.x,c.y);ctx.stroke()})});
  if(mode==="military")stxDSBases().filter(b=>b.status==="operational"&&["military","sensor"].includes(b.type)).forEach(b=>{const s=worldToScreen(b.x,b.y),radius=(b.type==="sensor"?780+(b.tier||1)*360:520+(b.tier||1)*280)*state.camera.zoom;ctx.strokeStyle=b.type==="sensor"?"rgba(169,140,255,.23)":"rgba(255,120,111,.23)";ctx.setLineDash([6,9]);ctx.beginPath();ctx.arc(s.x,s.y,radius,0,6.283);ctx.stroke()});
  state.deepSpaceOperations.filter(o=>o.active!==false).forEach(o=>{if(!visible(o.x,o.y,o.radius))return;const s=worldToScreen(o.x,o.y),r=o.radius*state.camera.zoom;ctx.strokeStyle=o.owner===0?"rgba(255,180,80,.48)":"rgba(255,76,101,.52)";ctx.fillStyle=o.owner===0?"rgba(255,180,80,.025)":"rgba(255,76,101,.03)";ctx.setLineDash([4,6]);ctx.beginPath();ctx.arc(s.x,s.y,r,0,6.283);ctx.fill();ctx.stroke();ctx.setLineDash([]);if(state.camera.zoom>.55){ctx.fillStyle=o.owner===0?"#ffc565":"#ff7287";ctx.font="800 8px system-ui";ctx.textAlign="center";ctx.fillText(`${o.kind==="blockade"?"BLOCKADE":"COMMERCE RAID"} · ${o.intercepted} INTERCEPTED`,s.x,s.y-r-7)}});ctx.setLineDash([]);ctx.restore();
}
function stxDSFormationPoint(center,i,count,seed,t,travelAngle=null){
  const row=Math.floor(Math.sqrt(i)),col=i-row*row,a=(i/count)*6.283+seed*.001,spread=8+Math.sqrt(count)*3;if(travelAngle!==null){const forward=(row-Math.sqrt(count)/2)*8,lateral=(col-row/2)*9,wobble=Math.sin(t*1.4+i)*2;return{x:center.x+Math.cos(travelAngle)*forward-Math.sin(travelAngle)*(lateral+wobble),y:center.y+Math.sin(travelAngle)*forward+Math.cos(travelAngle)*(lateral+wobble)}}return{x:center.x+Math.cos(a+t*.12*(i%2?1:-1))*spread*(.45+(i%5)*.16),y:center.y+Math.sin(a+t*.12*(i%2?1:-1))*spread*.58*(.45+(i%5)*.16)}}
function stxDSDrawFleetFormations(){
  const z=state.camera.zoom,t=performance.now()/1000,busy=new Set;let craftBudget=z<.94?120:200;state.battles.forEach(b=>{(b.attackerFleetIds||[]).forEach(id=>busy.add(id));(b.defenderFleetIds||[]).forEach(id=>busy.add(id))});state.deepSpaceBattles.forEach(b=>{b.attackerFleetIds.forEach(id=>busy.add(id));b.defenderFleetIds.forEach(id=>busy.add(id))});
  state.fleets.filter(f=>!f.destroyed&&!busy.has(f.id)&&(f.owner===0||typeof stxFleetVisible!=="function"||stxFleetVisible(f))).forEach(f=>{if(craftBudget<=0)return;const pos=typeof stxFleetPosition==="function"?stxFleetPosition(f):stxDSCurrentFleetPoint(f);if(!pos||!visible(pos.x,pos.y,100))return;const center=worldToScreen(pos.x,pos.y),ship=state.ships.find(s=>s.fleetId===f.id),color=empire(f.owner)?.color||"#fff",seed=stxDSHash(f.id),damage=clamp((f.strength||0)/Math.max(f.maxServiceStrength||f.strength||1,1),0,1);if(z<.46){if(ship)return;craftBudget--;ctx.save();ctx.translate(center.x,center.y);ctx.fillStyle=color;ctx.beginPath();ctx.moveTo(8,0);ctx.lineTo(-4,-5);ctx.lineTo(-2,0);ctx.lineTo(-4,5);ctx.closePath();ctx.fill();ctx.restore();return}const desired=z<.94?clamp(Math.ceil(Math.sqrt(Math.max(1,f.strength))*1.15),3,8):clamp(Math.ceil(Math.sqrt(Math.max(1,f.strength))*2.3),8,24),n=Math.min(desired,craftBudget),angle=ship?Math.atan2((ship.targetY??state.planets.find(p=>p.id===ship.to)?.y??ship.y)-ship.y,(ship.targetX??state.planets.find(p=>p.id===ship.to)?.x??ship.x)-ship.x):null;craftBudget-=n;for(let i=0;i<n;i++){if(damage<.55&&((Math.floor(t*7)+i+seed)%7===0))continue;const p=stxDSFormationPoint(center,i,n,seed,t,angle),target=angle===null?{x:center.x+Math.cos(t+i)*12,y:center.y+Math.sin(t+i)*12}:{x:p.x+Math.cos(angle)*14,y:p.y+Math.sin(angle)*14};drawCombatCraft(p,target,color,clamp((i===0?2.9:2.05)*z,1.8,5.5),i===0&&f.strength>=28)}if(damage<.72&&z>.85){ctx.save();ctx.fillStyle="#ff956d";ctx.globalAlpha=.35+.3*Math.sin(t*6+seed);ctx.fillRect(center.x+Math.sin(t+seed)*12,center.y-7,2,2);ctx.restore()}});
}
function stxDSBattlePoints(center,r,count,direction,time){const out=[];for(let i=0;i<count;i++)out.push(battlePoint(center,r,i,count,direction,time));return out}
function stxDSDrawBattleScene(b,center,base=null){
  const z=state.camera.zoom,ac=empire(b.attacker)?.color||"#ff667c",dc=empire(b.defender)?.color||"#67b7ff",r=clamp(94*z,36,142),pulse=.5+.5*Math.sin(b.elapsed*7);if(z<.34){ctx.strokeStyle=ac;ctx.beginPath();ctx.arc(center.x,center.y,13+pulse*5,0,Math.PI);ctx.stroke();ctx.strokeStyle=dc;ctx.beginPath();ctx.arc(center.x,center.y,13+pulse*5,Math.PI,6.283);ctx.stroke();return}const close=z>.9,min=close?8:4,max=close?24:12,an=clamp(Math.ceil(Math.sqrt(Math.max(1,b.attackerInitial))*2.05),min,max),dn=clamp(Math.ceil(Math.sqrt(Math.max(1,b.defenderInitial))*2),min,max),ap=stxDSBattlePoints(center,r,an,1,b.elapsed),dp=stxDSBattlePoints(center,r*.76,dn,-1,b.elapsed),shots=Math.min(12,Math.max(6,Math.ceil((an+dn)/3)));ctx.save();ctx.globalCompositeOperation="lighter";for(let i=0;i<shots;i++){const a=ap[(i*3+Math.floor(b.elapsed*2))%ap.length],d=dp[(i*5+1)%dp.length],phase=(b.elapsed*(3.6+(i%3)*.35)+i*.21)%1;if(phase>.68)continue;const origin=i%2?a:d,target=i%2?d:a,tx=origin.x+(target.x-origin.x)*phase,ty=origin.y+(target.y-origin.y)*phase;ctx.strokeStyle=i%2?ac:dc;ctx.globalAlpha=.34+.52*(1-phase);ctx.lineWidth=i%4===0?2:1;ctx.beginPath();ctx.moveTo(origin.x,origin.y);ctx.lineTo(tx,ty);ctx.stroke();ctx.fillStyle=i%5===0?"#ffdf82":"#fff";ctx.beginPath();ctx.arc(tx,ty,i%4===0?2.4:1.3,0,6.283);ctx.fill()}if(base&&base.status==="operational"&&dp.length){for(let i=0;i<Math.min(3,1+(base.tier||1));i++){const d=dp[(Math.floor(b.elapsed*2)+i*3)%dp.length];ctx.strokeStyle=stxDSBaseSpec(base).color;ctx.globalAlpha=.5;ctx.lineWidth=2.2;ctx.beginPath();ctx.moveTo(center.x,center.y);ctx.lineTo(d.x,d.y);ctx.stroke()}}ctx.restore();const ar=clamp(b.attackerStrength/Math.max(1,b.attackerInitial),0,1),dr=clamp(b.defenderStrength/Math.max(1,b.defenderInitial),0,1);ap.forEach((p,i)=>{if(ar<.45&&((i+Math.floor(b.elapsed*8))%6===0))return;drawCombatCraft(p,dp[i%dp.length]||center,ac,clamp((i===0?3:2.2)*z,1.9,5.6),i===0)});dp.forEach((p,i)=>{if(dr<.45&&((i+Math.floor(b.elapsed*7))%6===0))return;drawCombatCraft(p,ap[i%ap.length]||center,dc,clamp((i===0?2.9:2.1)*z,1.8,5.4),i===0)});ctx.globalAlpha=.8;ctx.lineWidth=2;ctx.strokeStyle=ac;ctx.beginPath();ctx.arc(center.x,center.y,r+14,-1.57,-1.57+6.283*ar);ctx.stroke();ctx.strokeStyle=dc;ctx.beginPath();ctx.arc(center.x,center.y,r+19,-1.57,-1.57+6.283*dr);ctx.stroke();ctx.globalAlpha=1;
}
drawBattles=function(){
  let shown=0;for(const b of state.battles){if(shown>=5)break;const p=state.planets.find(x=>x.id===b.planetId);if(!p||!visible(p.x,p.y,220))continue;shown++;stxDSDrawBattleScene(b,worldToScreen(p.x,p.y),null)}
  for(const b of state.deepSpaceBattles){if(shown>=5)break;const base=stxDSBase(b.baseId);if(!base||!visible(base.x,base.y,220))continue;shown++;stxDSDrawBattleScene(b,worldToScreen(base.x,base.y),base);if(state.camera.zoom>.5){const s=worldToScreen(base.x,base.y);ctx.fillStyle="#fff";ctx.font="900 9px system-ui";ctx.textAlign="center";ctx.fillText(`BATTLE OF THE ${base.region.toUpperCase()}`,s.x,s.y-145*state.camera.zoom)}}
};
function stxDSDrawPlanetIdentity(){
  if(state.camera.zoom<.62)return;const z=state.camera.zoom,t=performance.now()/1000;let budget=32;for(const p of state.planets){if(budget<=0||!visible(p.x,p.y,100)||p.owner===null)continue;const s=worldToScreen(p.x,p.y),r=clamp(p.r*z,5,36),color=empire(p.owner)?.color||"#fff",seed=stxDSHash(p.id);ctx.save();if(p.specialization==="Industrial Center"){for(let i=0;i<3;i++){ctx.fillStyle=i===1?"#ffb45d":"#70dfff";ctx.globalAlpha=.35+.25*Math.sin(t*2+i+seed);ctx.fillRect(s.x-r*.55+i*r*.5,s.y+r*.05+(i%2)*3,2.2,2.2)}budget--}else if(p.specialization==="Mining World"){const a=t*.18+seed*.01,x=s.x+Math.cos(a)*r*2.1,y=s.y+Math.sin(a)*r*.7;ctx.strokeStyle="#b8c8da";ctx.strokeRect(x-4,y-2,8,4);ctx.beginPath();ctx.moveTo(x-6,y);ctx.lineTo(x+6,y);ctx.stroke();budget--}else if(["Trade Hub","Free Port"].includes(p.specialization)){for(let i=0;i<3;i++){const a=t*(.22+i*.015)+i*2.1+seed*.01;ctx.fillStyle=i===0?"#ffd56f":"#75eaff";ctx.beginPath();ctx.arc(s.x+Math.cos(a)*r*(2.2+i*.2),s.y+Math.sin(a)*r*(.72+i*.06),1.5+i*.2,0,6.283);ctx.fill()}budget-=2}else if(p.specialization==="Military Stronghold"){for(let i=0;i<2;i++){const a=t*(i?-.24:.2)+i*3.1+seed*.01,x=s.x+Math.cos(a)*r*2.15,y=s.y+Math.sin(a)*r*.72;drawCombatCraft({x,y},{x:x-Math.sin(a)*10,y:y+Math.cos(a)*10},color,2.2,false)}budget-=2}else if(p.specialization==="Research World"){ctx.strokeStyle="rgba(169,140,255,.35)";ctx.beginPath();ctx.arc(s.x,s.y,r*(1.4+(t*.18%1)),0,6.283);ctx.stroke();budget--}else if(p.specialization==="War-Torn World"){ctx.fillStyle="#ff765f";ctx.globalAlpha=.35+.5*Math.sin(t*8+seed);ctx.beginPath();ctx.arc(s.x+r*.2,s.y-r*.15,2.2,0,6.283);ctx.fill();budget--}ctx.restore()}
}
const STX_DS_draw=draw;
draw=function(){STX_DS_draw();stxDSDrawNetworks();stxDSBases().forEach(stxDSDrawBase);stxDSDrawFleetFormations();stxDSDrawPlanetIdentity()};

function stxDSInstallStyles(){
  if($("stxDSStyles"))return;const style=document.createElement("style");style.id="stxDSStyles";style.textContent=`
  .stx-ds-planet-block{margin-top:16px}.stx-ds-project{border-color:rgba(105,239,255,.3)!important;background:linear-gradient(145deg,rgba(105,239,255,.05),rgba(6,12,26,.55))!important}.stx-ds-materials{display:flex;flex-wrap:wrap;gap:5px;margin:8px 0}.stx-ds-materials span{padding:4px 6px;border:1px solid rgba(255,255,255,.1);border-radius:6px;color:#ffc96b;font:700 9px system-ui}.stx-ds-materials span.ready{color:#71edaa;border-color:rgba(113,237,170,.3)}.stx-ds-transit{display:flex;width:100%;justify-content:space-between;gap:8px;padding:6px 7px;margin-top:5px;border:1px solid rgba(105,239,255,.13);border-radius:7px;background:rgba(3,10,22,.62);color:#eaf8ff;text-align:left;cursor:pointer}.stx-ds-transit b{font:700 9px system-ui}.stx-ds-transit span{font:600 8px system-ui;color:#8fa7bd;text-align:right}.stx-ds-build-grid{display:grid;grid-template-columns:1fr 1fr;gap:6px}.stx-ds-build-grid button{display:flex;flex-direction:column;align-items:flex-start}.stx-ds-build-grid small{margin-top:2px;color:#8198b1;font-size:8px}.stx-ds-base-panel{margin-bottom:14px;padding:13px;border:1px solid rgba(105,239,255,.24);border-left:3px solid var(--ds-color);border-radius:12px;background:linear-gradient(145deg,rgba(24,42,67,.35),rgba(4,9,21,.86));box-shadow:0 0 22px rgba(76,184,255,.06)}.stx-ds-base-panel.wreck{border-color:rgba(255,91,112,.3);background:linear-gradient(145deg,rgba(112,29,45,.16),rgba(4,9,21,.9))}.stx-ds-base-panel h3{margin:5px 0;color:var(--ds-color)}.stx-ds-base-head{display:flex;justify-content:space-between;gap:12px}.stx-ds-base-head small{color:#879eb4;font:600 9px system-ui}.stx-ds-base-head>b{color:var(--ds-color);font:900 10px system-ui}.stx-ds-pressure{margin:12px 0;padding:10px;border:1px solid rgba(255,184,80,.25);border-radius:10px;background:rgba(255,184,80,.055)}.stx-ds-pressure.hostile{border-color:rgba(255,78,105,.34);background:rgba(255,78,105,.075)}.stx-ds-placement{width:min(650px,94vw);max-height:88vh;overflow:auto;border-radius:20px;padding:22px}.stx-ds-site{display:flex;width:100%;flex-direction:column;gap:3px;margin:7px 0;padding:12px;border:1px solid rgba(105,239,255,.2);border-radius:10px;background:rgba(20,45,72,.2);color:#eefaff;text-align:left;cursor:pointer}.stx-ds-site:hover{border-color:rgba(105,239,255,.55);background:rgba(52,124,160,.14)}.stx-ds-site span{color:#70e9ff;font:700 9px system-ui}.stx-ds-site small{color:#91a6bb}.stx-ds-target{border-color:rgba(255,104,111,.26)!important}.stx-ds-target small{color:#ffac9e!important}.stx-ds-incident{border-color:rgba(255,152,74,.36)!important}.stx-ds-peace{border-color:rgba(104,239,173,.35)!important}.stx-ds-diplomacy{margin-top:12px;padding:13px;border:1px solid rgba(105,239,255,.18);border-radius:12px;background:linear-gradient(145deg,rgba(105,239,255,.045),rgba(4,9,22,.78))}.stx-ds-agreement-tags{display:flex;flex-wrap:wrap;gap:5px;margin:8px 0}.stx-ds-agreement-tags span{padding:4px 7px;border:1px solid rgba(105,239,255,.24);border-radius:999px;color:#8cefff;font:800 8px system-ui}.stx-ds-agreement-tags span.hostile{border-color:rgba(255,89,110,.32);color:#ff8c9d}.stx-ds-war-goal{display:grid;grid-template-columns:1fr auto;gap:3px 10px;margin:8px 0;padding:9px;border:1px solid rgba(255,94,113,.22);border-radius:8px;background:rgba(255,65,94,.06)}.stx-ds-war-goal label{grid-column:1/-1;color:#ff8898;font:800 8px system-ui;letter-spacing:.1em}.stx-ds-war-goal b{color:#f4f7ff;font-size:10px}.stx-ds-war-goal span{color:#ffad78;font:800 8px system-ui}.stx-ds-memory{display:flex;justify-content:space-between;gap:8px;padding:5px 0;border-bottom:1px solid rgba(255,255,255,.05);font:600 9px system-ui}.stx-ds-memory b.good{color:#72edaa}.stx-ds-memory b.bad{color:#ff7f91}.stx-ds-agreement-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:5px;margin-top:8px}@media(max-width:720px){.stx-ds-build-grid,.stx-ds-agreement-grid{grid-template-columns:1fr}.stx-ds-base-head{flex-direction:column}.stx-ds-base-panel .stx-ol-stat-grid{grid-template-columns:1fr 1fr}}`;
  document.head.appendChild(style);
}
stxDSInstallStyles();
